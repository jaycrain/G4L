# Screenshots — provenance

**Captured at v3.5.48 (2026-08-29) and carried forward into the v3.5.59 bundle deliberately.**

None of the releases in this bundle (v3.5.49–v3.5.59) changes any surface shown here. They alter onboarding
capture behaviour, the intake scope gate, and the Reclaim List builder's handling of a multi-want line — none of
which is visible on the front door, login, dashboard, Program, Playbook, Reclaim List or Momentum pages.

Saying so explicitly because a re-used screenshot that looks current is exactly the sort of thing that quietly
misleads: if these were stale in a way that mattered, this file is where it would be recorded.

## Not captured in this bundle

**The Reclaim builder's split proposal** (§2 of the sync note) — *"That sounds like more than one thing. Add them
separately?"* with **Add as N separate** / **Keep as one**. It appears only mid-onboarding, and only when a member
types a line carrying several wants, so it cannot be reached by walking to a URL. The copy is authored and is in
`member-transcript.md`; quote it from there. Ask if you need an image and I will stage one.
