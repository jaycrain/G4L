# Screenshots — provenance

**Carried forward from v3.7.3, where all 11 were captured fresh against production earlier the same day
(2026-09-02).** Byte-identical to that set.

v3.7.4 changes engine behaviour only — no member-facing string, layout or surface differs from v3.7.3 — so these
remain accurate. Said explicitly rather than left to infer: a re-used screenshot that looks current is exactly
the sort of thing that quietly misleads, and this file is where it would be recorded if it mattered.

`04-program.jpg` is the one to reach for. It shows the whole of Cycle 1 in a member's reading order — four
phases, every Session, every descriptor — and it is the best artifact we have for describing the program's shape.

## Still not capturable by walking to a URL

**The Reclaim builder's split proposal** — *"That sounds like more than one thing. Add them separately?"* with
**Add as N separate** / **Keep as one**. It appears only mid-onboarding, and only when a member types a line
carrying several wants. The copy is authored and is in `member-transcript.md`; quote it from there. Ask and I
will stage one.
