# Screenshots — provenance

**All 11 captured FRESH against production (https://g4l-ten.vercel.app) on 2026-09-02 at v3.7.3 (`0a3ddc14`).**
Nothing in this set is carried forward from an earlier bundle.

Recorded explicitly because the previous two bundles DID carry theirs forward, and because a re-used screenshot
that looks current is exactly the sort of thing that quietly misleads. If these were ever stale in a way that
mattered, this file is where it would be said.

## The gap from v3.7.2 is closed

That bundle shipped with `04-program` DELETED rather than stale — it still showed the Session named "IDQ", which
is precisely what v3.7.2 renamed. It is captured here, showing **The Distance** with its descriptor, alongside
every other Session in Cycle 1.

## Still not capturable by walking to a URL

**The Reclaim builder's split proposal** — *"That sounds like more than one thing. Add them separately?"* with
**Add as N separate** / **Keep as one**. It appears only mid-onboarding, and only when a member types a line
carrying several wants. The copy is authored and is in `member-transcript.md`; quote it from there. Ask and I
will stage one.
