# v3.2.1 Failure Catalog — adversarial discovery sweep

_Generated 2026-07-29 · run wf_04b67a5b-f42 · 20 adversarial walkers · 81 raw findings -> 53 unique · DISCOVERY ONLY (no fixes applied)._

The whole map of what a Charter member could hit, found by fanning out independent adversarial walkers across the member surface (offline engine reproduction + static audit) — not by walking one-at-a-time. Jay's walk items are absorbed here: IDP-2 = the CAT-01..05 cluster; IDP-1 = CAT-44; IDP-3 = CAT-13/14. Fix by **class**, not instance.

## Tally

| Severity | Count |
|---|---|
| Governance (member harmed/turned away, or AI-gov rule) | 13 |
| Data (wrong/lost/inconsistent member data) | 19 |
| Functional (flow breaks/stalls/loops) | 16 |
| Cosmetic (visual only) | 13 |
| **Total unique** | **53** |

## Completeness critic

Confidence in catalog completeness: **medium**

**Surfaces under-covered (need a follow-up probe):**
- Authentication & session lifecycle (app/login/actions.ts, lib/auth/store.ts, app/auth.ts) — NOT touched by any slice. Verified gaps: (a) no login rate-limiting/lockout/throttle on credential check; (b) changePasswordAction (app/account/actions.ts:68-79) rotates the hash but does NOT delete/rotate other sessions — a compromised or shared session survives a password change; (c) session token fixation/rotation on privilege change never probed; (d) SESSION_TTL sliding/expiry behavior unexercised.
- Authorization / IDOR boundary end-to-end. 14 arc PAGE shells (app/{reconnect,rewire,rebuild,reclaim}/[memberId]/**) render on the raw URL memberId with NO authorizeMember call at the page level (auth deferred to actions). Actions are guarded, but the page-render + session-resume authz (loadArcSession / ReconnectChat mount reading another member's in-progress state) was never probed as a unit. Admin console (app/admin/**) and the member-diagnostic endpoint token model got zero coverage.
- Connect / community authorization + moderation (app/connect/actions.ts, room pages, app/api/connect/rooms/[roomId]/route.ts). The connect slices audited ONLY render crash-safety. Never probed: IDOR on /connect/[memberId] vs session actor; room-membership enforcement (RoomPage authorizes the URL memberId but not room participation); cheer/checkIn/report on arbitrary post/reply/message IDs; block/report abuse; crisis-routing on posts+replies+room messages as the live governance gate.
- Cron / outreach delivery endpoints (app/api/cron/{nudge,activity,ai-health}/route.ts). Guard is `if (secret && ...)` — if CRON_SECRET is UNSET on prod the nudge-send loop and Strava sync are world-callable. The config-flag slice checked product flags only, never CRON_SECRET presence on prod.
- Proactive Outreach ENGINE (lib/outreach/*: cadence, backoff-on-ignored, quiet-hours, per-member content generation, plan-offer, Dial-4 distress content; lib/push/{cron,send,payload}). Zero engine-replay coverage — who gets nudged, what the GENERATED copy says, whether governance/crisis/no-double-nudge/consent hold in a real nudge. Companion-visibility slice only checked read-context assembly, not the outbound decision.
- Account & data-rights surface (app/account/*: profile-form, avatar-upload, notification-prefs, connect-settings, password-form; right-to-erasure delete paths). Avatar upload (file type/size/SSRF/storage), notification-pref writes, and the erasure/account-close flow (the session-wipe at actions.ts:117) never exercised.
- Strava OAuth activity path (app/api/activity/strava/{callback,connect}, lib/activity/*) — state/CSRF on the OAuth callback, token storage, health-data isolation. Flagged security-review-pending in MEMORY, still unprobed by this sweep.
- Mobile / PWA / offline runtime (public/sw.js, app/offline, app/manifest.ts, app/pwa-client.tsx, app/push). MOBILE parked, but the service worker still ships on prod (MEMORY notes a dev SW caching stale CSS). SW cache-invalidation on deploy, offline-page auth state, and push-subscription lifecycle unprobed.
- The always-on Companion RUNTIME (checkinReply execution, tool-executor side effects, degraded-turn behavior under live model). companion-visibility audited context ASSEMBLY only; the actual reply + tool-call mutation path + crisis short-circuit inside checkin was not exercised.
- Score / IDQ standalone surfaces (app/idq, app/score/[memberId], /grinta) and the 60-day IDQ retake cadence — the frozen data contract's re-administration path was not walked by any slice.
- Field-guide, Story, Journey, Playbook, Movement, Momentum subpages as READ surfaces — only Playbook/Reclaim were incidentally touched via the triptych; these member-visible pages were not individually audited for empty-state/auth/MA-parity.

**Failure classes possibly missed:**
- missing-rate-limit-on-auth-endpoint — no lockout/throttle on login credential verification (credential stuffing / brute force).
- session-not-invalidated-on-credential-change — password change leaves other sessions live (no rotate/revoke).
- page-render-authz-deferred-to-action — [memberId] page shells render before any ownership check; only mutations are guarded, leaving resume/read-state IDOR unverified.
- conditional-secret-guard-fails-open — `if (secret && ...)` on cron routes means an unset secret disables the guard entirely (fail-open), unmeasured against prod env.
- idor-on-url-scoped-resource — member-scoped Connect/room/cheer/report IDs never adversarially cross-tested against a foreign session.
- governance-in-generated-outbound-copy-unverified — crisis/no-diagnose/no-auto-send posture holds for READ context but the OUTBOUND nudge/email copy generation was never checked.
- oauth-callback-state-csrf-unverified — Strava callback state/CSRF/token-binding not probed.
- file-upload-unvalidated — avatar upload type/size/storage-path/SSRF surface unexamined.
- service-worker-stale-cache-on-deploy — SW cache-bust on new deploy (a known dev-tell) never verified for prod.
- runtime-tool-executor-side-effects — the Companion's live tool-call mutations (vs. pure context render) unexercised.
- cross-device-concurrent-write-lost-update — multiple slices explicitly flagged concurrent onboarding/arc saves racing the same row as unverified (on-conflict-do-update lost update).
- arc-session-resume-round-trip-fidelity — saveArcSession/loadArcSession expects+scratch round-trip through the ::text::jsonb double-encode path flagged unverified by rewire/rebuild/reconnect slices; the resume path is the most likely place a scale turn re-shows a free-text box.

**Recommended next probes:**
- AUTHZ IDOR sweep: with two real member sessions (A, B), attempt to GET every /X/[memberId] page and POST every server action with B's id while authed as A. Confirm authorizeMember fires on page RENDER (not just mutation) for the 14 arc pages, /connect/[memberId], /badges, /score, /grinta, /journey, /field-guide, /reclaim-list, and the admin diagnostic token endpoint.
- Session-lifecycle probe: verify changePasswordAction invalidates OTHER sessions (currently it does not — app/account/actions.ts:77 rotates hash then returns; add member_session delete + regression). Add a login rate-limit/lockout probe (N failed attempts). Test session-token rotation on login and account-close.
- Cron fail-open check: query prod env (via Jay / Vercel) for CRON_SECRET presence. If unset, /api/cron/nudge, /activity, /ai-health are world-callable — an unauth GET fires the nudge/Strava loop. Harden to require the secret (fail-closed) and re-probe.
- Connect authz + moderation replay: cross-member cheer/checkIn/report/block on arbitrary IDs; room-message send/report without room membership; verify RoomPage enforces participation not just URL memberId; confirm detectCrisis fires on post, reply, AND room message with the 988 route.
- Outreach ENGINE replay harness (mirror the onboarding replay pattern): drive lib/outreach/engine over personas to assert cadence ceiling, backoff-on-ignored to the weekly floor, quiet-hours suppression, no-double-nudge, consent gate, and — critically — that a GENERATED nudge cannot advise/diagnose and routes distress to 988. Verify Founder-Agent no-auto-send holds for any Jay-voiced outbound.
- Companion RUNTIME probe: exercise checkinReply live (or offline with a scripted model turn) to test the tool-executor side effects (does a member ask mutate Reclaim/commitments correctly via propose-confirm-commit?) and the crisis short-circuit + degraded-context turn wording.
- Arc/onboarding resume round-trip: save an in-flight scale/administered turn, reload via loadArcSession/loadOnboardingSession through the real ::text::jsonb path, assert expects re-derives to kind:'scale' (chips, not a free-text box) and scratch survives — the resume regression multiple slices flagged.
- Concurrent-write test: two devices submitting onboardingTurn / arc turns to the same email/member row; assert no lost-update on the on-conflict-do-update path.
- Strava OAuth security walk: verify the callback validates state/CSRF and binds tokens to the session member; audit avatar-upload for type/size/path validation. (Both are MEMORY-flagged security-review-pending.)
- PWA/SW deploy probe: confirm public/sw.js cache versioning busts on a new deploy (the MEMORY-noted stale-CSS tell) and that /offline + push subscriptions handle an expired session cleanly.
- 60-day IDQ retake walk: exercise the re-administration cadence end-to-end (app/idq, /score) and confirm the frozen scoring contract + trend write are correct on a second take — no slice covered the retake.


---

## Governance

### [CAT-01] Decline/scope gate keys on a narrow loss-verb vocabulary and ignores the committed Doors — real Fade members (Doors-accumulation, Acceptance) wrongly declined
- **Severity/Confidence:** governance · reproduced · merged 3 dupes
- **Class:** `proxy-vocabulary-scope-gate`
- **Surface:** Onboarding gap stage — Decision-E thriving-decline fork (onboarding-staged.ts:1171-1191)
- **Symptom:** A member with a genuine identity Fade expressed through a Door (full_house, career_cliff, grind, marriage, load_bearer, vanishing, body) or through resigned Acceptance, but WITHOUT loss-verb vocabulary, is served the DECLINE_REPLY ('you're not carrying the kind of distance this program is built for… keep building') and terminated with no card and no Reclaim List. This turns away the exact demographic G4L exists for — and even overrides a model that CORRECTLY committed collected.doors=['acceptance'].
- **Root cause:** noRealFadeSignal = !isAcceptanceFade(gapCorpus) && !hasGenuineLoss(gapCorpus) (onboarding-staged.ts:1179). hasGenuineLoss (onboarding-intent.ts:158-161) keys ONLY on LOSS_RE loss verbs and deliberately ignores matchDoors; isAcceptanceFade (intent.ts:178-180) re-parses raw text via matchDoors and never consults b.collected.doors (which augmentDoors only populates AFTER the decline decision, staged:1191). isForwardAmbition then fires on ambition tokens ('bigger job','thriving','level up','feel ambitious again') that restless-but-genuine faders use. The 8 Doors ARE the fade taxonomy yet the gate consults only one of them, from re-parsed text rather than committed state.
- **Reproduction:** /tmp/sweep-decline/probe4.ts (full-house + retirement faders DECLINED at turn 1); /tmp/sweep-acceptance/probe10.ts (committed doors=['acceptance'] still declined)
- **Evidence:** onboarding-staged.ts:1171,1179-1191; onboarding-intent.ts:154-166,178-180; doors.ts:75 (acceptance alias list missing 'too old'/'no point'/'come to terms'/'over the hill'); repro /tmp/sweep-doors probe.ts/probe2.ts, /tmp/sweep-acceptance probe7/probe8/probe10, /tmp/sweep-decline probe3.ts/probe4.ts

### [CAT-02] note_no_fade model contract is a one-case signal feeding a two-case engine fork (resigned-Acceptance vs thriving), keyed on the same 'want more' words real faders use
- **Severity/Confidence:** governance · high · merged 2 dupes
- **Class:** `model-engine-contract-drift`
- **Surface:** note_no_fade tool spec + STAGED_SYSTEM prompt (onboarding-staged.ts:1586-1591, 1750-1754) vs Decision-E fork
- **Symptom:** The engine fork is two-case (RESIGNED→Acceptance served vs THRIVING→declined) but the model-facing note_no_fade instruction collapses both into 'no loss + no drift = note_no_fade' and is described purely by forward-desire vocabulary ('thriving and simply want MORE, optimize, level up, next challenge') with no fade-first check. A midlife member with a real Fade who frames their want as 'I want to level up / feel ambitious again' matches the trigger, so the model fires note_no_fade on genuine faders, then the fragile engine regex (CAT-01) declines or strands them. The model has no way to signal 'no discrete event BUT resigned = serve' or 'they want more AND carry a loss'.
- **Root cause:** The two-way scope decision lives only in the engine's DECLINE_REPLY comment/regexes; the model contract was never updated to distinguish resigned-served-Acceptance from thriving-declined, and AMBITION_RE/the tool text encode 'presence of ambition' as decisive rather than 'absence of loss'. Forward desire and a real Fade are orthogonal but the tool conflates them.
- **Reproduction:** Code-path + contract reading cross-checked against /tmp/sweep-decline/probe4.ts and /tmp/sweep-acceptance/probe7,probe10
- **Evidence:** onboarding-staged.ts:259-269 (two-case fork comment), 1586-1591 (note_no_fade description), 1750-1754 (single-rule prompt, no resignation carve-out), 1586/1675 (boolean carries no served-vs-declined distinction); onboarding-intent.ts:148-149 (AMBITION_RE); supporting repro /tmp/sweep-decline/probe4.ts

### [CAT-03] Genuinely thriving no-fade member is force-ADMITTED with a fabricated Fade — the GAP_MAX_TURNS corpus-grab has no symmetric decline defense
- **Severity/Confidence:** governance · reproduced · merged 2 dupes
- **Class:** `asymmetric-backstop`
- **Surface:** Onboarding gap stage — GAP_MAX_TURNS never-strand corpus grab (onboarding-staged.ts:1165-1168)
- **Symptom:** A forward optimizer with zero loss ('wonderful family, career I love, feel fulfilled, nothing missing') is NOT declined; their happy corpus is captured verbatim as their 'gap/fade story', they advance to reclaim, and the Companion tells them 'no wonder the Builder got quiet under all of it' — manufacturing a hardship they don't have, the exact governance breach the locked-scope guard exists to prevent. Even an explicit 'nothing went wrong, no loss, no drift, I just miss my old intensity' is admitted when an incidental nostalgia token ('miss','used to','burned out') is present.
- **Root cause:** The corpus grab fires on `corpus.length>=40 && !isForwardAmbition(corpus)` with NO loss/Door requirement (unlike shouldCaptureStagedGap which requires isRealFade && hasLossSignal), so absence of an ambition keyword fabricates a gap; once a gap exists the decline can never fire. Separately LOSS_RE includes soft nostalgia tokens and hasGenuineLoss unconditionally overrides isForwardAmbition (intent.ts:164), so one incidental token flips a self-declared no-fade optimizer to real-fade. The backstop layer defends ADMIT against model under-tagging but has no symmetric DECLINE defense; absence-of-fade is never itself sufficient to decline but IS sufficient to fabricate one.
- **Reproduction:** /tmp/sweep-nofade/probe7.ts (thriving member → stage 'reclaim', gap = happy corpus); /tmp/sweep-decline/probe7.ts ('miss the intensity' optimizer captured as fade)
- **Evidence:** onboarding-staged.ts:1165-1168,1179-1181; onboarding-intent.ts:152,154-165,122-128; repro /tmp/sweep-nofade/probe7.ts, /tmp/sweep-decline/probe7.ts/probe8.ts

### [CAT-04] Sticky note_no_fade misfire permanently strands a genuine-loss member — capture and decline both disabled, story dropped
- **Severity/Confidence:** governance · reproduced · merged 1 dupes
- **Class:** `sticky-flag-not-reconciled`
- **Surface:** Onboarding gap stage — note_no_fade handling (onboarding-staged.ts:1147)
- **Symptom:** If the model calls note_no_fade once (a fuzzy misjudgement) then draws out a genuine loss story without ever calling set_gap, the member loops forever on the same gap reflection, their fade is NEVER captured, and they can never reach Reclaim or the card. No decline, no completion — a dead loop that silently drops their whole loss story.
- **Root cause:** s.noFade is set stickily from a single model judgement (staged:1147), cleared only by isAcceptanceFade. Every backstop capture path is guarded by !s.noFade (staged:1154,1156,1162,1165), so once it sticks the engine can never infer the gap; meanwhile hasGenuineLoss is true so the thriving-decline also never fires. The member is caught between two gates that both refuse to act, with no re-evaluation of the stale flag against subsequently-revealed loss signal — 'a guess promoted to committed truth.'
- **Reproduction:** /tmp/sweep-decline/probe5.ts (note_no_fade turn 1, genuine caregiver-loss story never captured over 6 turns, no escape)
- **Evidence:** onboarding-staged.ts:1147,1154,1156,1162,1165,1174,1179; repro /tmp/sweep-decline/probe5.ts,probe6.ts

### [CAT-10] Coin-your-own identity captures arbitrary garbage verbatim as the rendered handle — no length/punctuation/emoji/article validity gate
- **Severity/Confidence:** governance · reproduced
- **Class:** `raw-input-promoted-to-committed-truth`
- **Surface:** Onboarding identity picker write-your-own → identity stage gather PICK-RESOLVE (onboarding-staged.ts:1057-1062)
- **Symptom:** Whatever the member types is committed verbatim as their identity handle and rendered as 'the X' on the card, dashboard, and every agent: 'the Athlete!'→'Athlete!', 'a runner 🏃'→'Runner 🏃', '"Athlete"'→'"athlete"' (lowercase-in-quotes), '- Athlete -', a 20-word run-on sentence title-cased, 'the'→'The', 'not really anyone tbh'→'Not Really Anyone Tbh'. All satisfy the completion contract (identityNoun non-empty).
- **Root cause:** The pick path is 'engine-authoritative, verbatim — never re-litigate' (staged:1044-1062) and trusts the raw field; cleanIdentityNoun (identity.ts:4) only strips a leading article and displayIdentityNoun (:11) only title-cases. Nothing trims trailing punctuation/quotes/emoji, caps length, or rejects a bare article. Free text that becomes a rendered label needs a validity gate with a re-prompt fallback, not blind commit — the same pattern onboarding hardening was built to kill, reintroduced by tap-to-pick.
- **Reproduction:** /tmp/sweep-idpick/probe.ts section 4 (enumerated garbage captures)
- **Evidence:** onboarding-staged.ts:1044-1062; lib/member/identity.ts:4-14; onboarding-contract.ts:46,85; repro /tmp/sweep-idpick/probe.ts §4

### [CAT-11] Same-turn offer_identity_words + name_identity silently commits an UNCONFIRMED identity and drops the chips (governance: names an identity without member confirmation)
- **Severity/Confidence:** governance · reproduced · merged 1 dupes
- **Class:** `no-mutual-exclusion-contract`
- **Surface:** Identity stage gather branch precedence (onboarding-staged.ts:1063 vs 1077)
- **Symptom:** When the model emits both offer_identity_words (candidates) and name_identity (a noun) on the same turn — the exact combination the tool docstrings forbid — the member never sees the chips and an identity they never picked ('the Rider') is committed straight into the follow-up probe. Also: once pendingIdentityPick is set, a same-turn model re-offer is discarded and the member's raw ambiguous message ('wait, Sprinter') is committed as the handle.
- **Root cause:** Gather branch precedence: the identityNoun branch (1063) and the pending-pick PICK-RESOLVE branch (1043) are evaluated BEFORE the identityCandidates offer branch (1077); parseStagedTurn sets both and mergeStaged commits the name unconditionally, with no contract that 'an offer this turn suppresses a same-turn name' or 'a pending pick can be superseded by a fresh offer.' Model and engine both act on the turn with no arbitration — violates the hard rule 'Never names an identity label without member confirmation.'
- **Reproduction:** /tmp/sweep-idpick/probe.ts §1 (candidates + name_identity 'Rider' → identityNoun='Rider' committed, chips dropped); §6 (pending pick consumes 'wait, Sprinter')
- **Evidence:** onboarding-staged.ts:1043,1057-1062,1063,1077,1536,1654-1658,1710; repro /tmp/sweep-idpick/probe.ts §1,§6

### [CAT-22] Deterministic crisis detector misses explicit self-harm/overdose/planning phrasings ('cutting myself', 'want to overdose', 'made a plan to end it')
- **Severity/Confidence:** governance · reproduced
- **Class:** `crisis-lexicon-coverage-hole`
- **Surface:** Governance crisis gate — detectCrisis pattern set (lib/agent/governance.ts:38-70)
- **Symptom:** detectCrisis returns not-flagged for 'cutting myself', 'cut myself last night', 'want to overdose', 'made a plan to end it', 'thinking about ending things', 'don't see a way out', 'nobody would miss me' — self-harm language is a required-route case and crisis routing is an always-on hard rule. The control 'hurting myself' IS flagged, proving a phrasing-specific gap, not a wiring failure.
- **Root cause:** The high-recall crisis pattern set covers 'hurt myself'/'self-harm' but has no pattern for the verb 'cut'/'cutting myself', 'overdose', or planning language — false negatives against its own stated bar. The deterministic gate is the reliable guarantee (the model prompt is only a second defense).
- **Reproduction:** /tmp/sweep-corrector-crisis/selfharm.ts (detectCrisis('cutting myself')=not flagged; 'want to overdose'=not flagged; control 'hurting myself'=flagged)
- **Evidence:** governance.ts:38-58,63-70; repro /tmp/sweep-corrector-crisis/selfharm.ts,crisis-and-reclaim.ts

### [CAT-24] Gap first-person governance rule is prompt-only — a third-person set_gap lands verbatim on the card/dashboard with no engine guard (risks a guessed-gender pronoun)
- **Severity/Confidence:** governance · high
- **Class:** `governance-rule-only-in-prompt`
- **Surface:** Onboarding gap capture — set_gap (onboarding-staged.ts:1663,1554-1559,1763-1767)
- **Symptom:** A third-person model set_gap reaches the card and dashboard verbatim, distancing the member from their own account and risking a guessed-gender pronoun — a stated hard governance harm — with the fuzzy model as the sole guardian.
- **Root cause:** parseStagedTurn accepts set_gap verbatim (mechanics only); the first-person rule lives only in the tool description and system prompt with no engine-layer detector or transform, unlike crisis routing and the fade gate which are enforced deterministically.
- **Reproduction:** Code-path reading: set_gap accepted verbatim; no person/pronoun guard anywhere in the engine
- **Evidence:** onboarding-staged.ts:1663,1554-1559,1763-1767; grep across onboarding-staged/intent/contract finds no person/pronoun enforcement

### [CAT-26] A declined (out-of-scope) session loses its terminal on resume and is dragged into a broken completion attempt with an empty collected
- **Severity/Confidence:** governance · reproduced · merged 1 dupes
- **Class:** `unlisted-terminal-stage-fallthrough`
- **Surface:** Onboarding kernel 'declined' stage + chat.tsx resume + finalize
- **Symptom:** A correctly-declined thriving no-fade member (stage='declined', warm terminal) is saved every turn; on resume the client restores the transcript with an ACTIVE input box and NO decline frame, and any message returns complete:true with the completion handoff. The card renders from an essentially empty collected, and 'This is me' then fails at finalize with the raw 'reclaim list must have at least 1 item (got 0)' — the graceful decline is silently converted into a confusing dead-end.
- **Root cause:** 'declined' is a terminal stage not enumerated in ARC.stageOrder/ARC.stages (1353-1358); on re-entry arc.stages['declined'] is undefined (1424) and falls through to the else 'CONFIRM-ONLY CARD' terminal branch (1470-1483) which unconditionally sets complete=true. beatState() (1012) drops the top-level declined flag and b.declined re-initialises to false each turn (1410), never restored from state, so the Turn can't signal it's declined; chat.tsx resume only special-cases stage==='complete' (158) and keys 'resumable' on messages.length>0 (85), both satisfied by a declined session.
- **Reproduction:** Offline applyStagedTurn: no-fade → stage='declined'; resume + any message → {complete:TRUE, empty collected}; runOnboarding → errors:['reclaim list must have at least 1 item (got 0)']
- **Evidence:** onboarding-staged.ts:1424,1470-1483,1410,1012-1030,1353-1358; chat.tsx:85,158,203-206; runOnboarding rejection; repro applyStagedTurn completion-persistence-audit

### [CAT-37] Earned badges (the 16-milestone Passport) are invisible to the always-on Member Agent — a cornerstone reconcile-with-MA violation
- **Severity/Confidence:** governance · reproduced
- **Class:** `member-visible-datum-not-known-to-MA`
- **Surface:** Dashboard Companion — CheckinContext + buildContext (checkin.ts:18-110; checkin-actions.ts:95-266)
- **Symptom:** The member earns up to 16 badges, sees them beside the centered Companion, and has one named at session close ('you just earned a badge: …'), yet the always-on Companion has NO badge data. Asked 'how many badges have I earned?' / 'what was that badge I just got?' it cannot answer, despite a system prompt asserting it holds everything about the member — and despite the hand-home explicitly cuing that it knows.
- **Root cause:** CheckinContext has no badge field and buildContext never reads earnedBadgeIds/getPassport, though the data exists and is surfaced on the dashboard, in the ceremony, at session close, and in the diagnostic JSON. Badges were wired to the redesign surfaces but the CLAUDE.md 'no data the member can see should be invisible to the agent' reconcile step was skipped for the companion.
- **Reproduction:** /tmp/sweep-companion/probe.ts (rendered MEMBER CONTEXT for an earned-badge member contains zero badge mentions; no `badges` field on CheckinContext)
- **Evidence:** checkin.ts:18-110,327; checkin-actions.ts:95-266; view.ts:96-175; triptych-left.tsx:131-145; diagnostic.ts:45; repro /tmp/sweep-companion/probe.ts

### [CAT-38] Companion degrade-to-minimal strips durable memory/story/Playbook/IDQ under one try/catch while the prompt forbids admitting any memory gap
- **Severity/Confidence:** governance · reproduced
- **Class:** `all-or-nothing-context-under-single-trycatch`
- **Surface:** Dashboard Companion — buildContext minimal fallback (checkin-actions.ts:79-90,91-271)
- **Symptom:** If ANY single supplementary read throws (a prod-drifted table, or a transient error), buildContext returns `minimal`, dropping agentMemory (people/relationships), pastSelf, gapStory, identityParagraph, the entire Playbook, Grinta Index, and all IDQ answers/dimensions/trend — while the system prompt HARD-FORBIDS admitting any memory gap ('NEVER say I don't carry memory between sessions'). On a degrade the agent is instructed to behave omnisciently on a gutted context, so it confabulates or goes generic and cannot honestly say what it lost.
- **Root cause:** The whole supplementary block is wrapped in one outer try/catch that returns minimal on ANY throw; the minimal fallback was scoped to keep the rail from 500-ing and later widened for momentum+commitments but durable memory/narrative/Playbook/IDQ were never added. The full path already uses per-field .catch on each Promise.all entry — that discipline was not applied to the fallback, and the prompt has no signal it's running degraded.
- **Reproduction:** /tmp/sweep-companion/degrade.ts (contextBlock on the minimal shape → LOST agentMemory/pastSelf/gapStory/Playbook/Grinta/IDQ)
- **Evidence:** checkin-actions.ts:79-90,91-94,267-271; checkin.ts:331-333; repro /tmp/sweep-companion/degrade.ts

---

## Data

### [CAT-06] matchDoors runs over a cross-stage corpus with no polarity check — happy past-self language and Reclaim wants fabricate phantom fade Doors; named Doors also dropped on stall/decline
- **Severity/Confidence:** data · reproduced · merged 3 dupes
- **Class:** `cross-stage-corpus-contamination`
- **Surface:** Onboarding gap stage — gapStageCorpus + augmentDoors/matchDoors (onboarding-staged.ts:795-797,1191,1221)
- **Symptom:** A clean single-Door member finishes onboarding with EXTRA fabricated Doors because their positive identity-stage description mentioned a Door alias ('my body strong'→body, 'raising kids and loving it'→full_house, 'the breadwinner and proud of it'→load_bearer, 'the grind never stopped me'→grind). Front-loaded Reclaim wants also leak in ('I want to feel strong in my body again'→body). Conversely, on any non-completing (declined/stalled/gap-wiped) path the real named Doors (grind, full_house, marriage) are never persisted — collected.doors undefined — because Door accumulation is coupled to a non-wiped gap.
- **Root cause:** gapStageCorpus (staged:795-797) joins EVERY member message regardless of stage — identity-stage past-self answers AND front-loaded reclaim wants — and augmentDoors→matchDoors (onboarding.ts:345; doors.ts:88-136) word-matches Door names/aliases with no polarity/context boundary, so positive descriptions and aspirations read as fade Doors. The inline comment at staged:794 wrongly claims 'Identity-stage answers don't false-match.' Meanwhile augmentDoors runs only inside `if (b.collected.gap)` (staged:1189-1191), so the FADE GATE wipe or a decline voids the Doors data contract entirely.
- **Reproduction:** /tmp/sweep-singleevent/probe8.ts (matchDoors enumeration of positive-language false matches); /tmp/sweep-terse-frontloader/door-leak.ts (want→'body')
- **Evidence:** onboarding-staged.ts:794-797,1189-1191,1221; onboarding.ts:345-351; doors.ts:88-136,136 (CONCRETE_PHYSICAL_EVENT); repro /tmp/sweep-singleevent/probe3.ts,probe8.ts, /tmp/sweep-terse-frontloader/door-leak.ts, /tmp/sweep-doors/probe3.ts,probe6.ts

### [CAT-07] correctDoors deletes a model-tagged aging_parents Door on any per-turn slice lacking a parent keyword — silent Door loss against the union contract
- **Severity/Confidence:** data · reproduced
- **Class:** `deterministic-heuristic-demotes-committed-data`
- **Surface:** Onboarding gap stage — augmentDoors/correctDoors (lib/doors.ts:167-187)
- **Symptom:** A member whose model correctly tagged BOTH full_house and aging_parents loses the aging_parents Door on the very first turn whenever that turn's narrative doesn't restate a parent keyword — contradicting augmentDoors' own 'union, never drop one' contract and CLAUDE.md's 'never drop what they gave you.'
- **Root cause:** correctDoors (doors.ts:167-187): when fullHouseSignal is true and the narrow agingParentsSignal regex (doors.ts:177) is false for the per-turn string, it executes set.delete('aging_parents') (line 182), pruning a Door the model explicitly recorded via note_door. augmentDoors runs correctDoors every gap turn against only the current gap-stage slice, so a keyword regex is trusted over the model's explicit tag — a guess demoting committed truth. tests/reclaim.test.ts:130 enshrines the drop, leaving the over-reach unguarded.
- **Reproduction:** /tmp/sweep-terse-frontloader/door-drop.ts (augmentDoors(['full_house','aging_parents'],'yeah it all piled up') → ['full_house'])
- **Evidence:** doors.ts:167-187 (esp. 182); onboarding.ts:345-351; onboarding-staged.ts:1191,1221; tests/reclaim.test.ts:130; repro /tmp/sweep-terse-frontloader/drop-repro.ts,door-drop.ts

### [CAT-08] note_door silently drops an invalid model slug (no log/nearest-match) and its description omits the 'acceptance' slug so The Acceptance is never model-tagged
- **Severity/Confidence:** data · reproduced · merged 1 dupes
- **Class:** `enum-slug-drift`
- **Surface:** Onboarding gap stage Door tagging — note_door tool (onboarding-staged.ts:1580-1581,1664)
- **Symptom:** The note_door description lists only 11 slugs while DOORS defines 12 (missing 'acceptance'), so the model is never told The Acceptance is a valid Door and cannot tag it — capture depends on a separate corpus heuristic. Separately, a plausible-but-invalid slug (e.g. 'divorce') is silently discarded with no log or nearest-match, so the model believes it recorded a Door that never landed.
- **Root cause:** The hardcoded slug list in the tool description drifted from the source-of-truth DOORS array when The Acceptance was added (doors.ts:23). parseStagedTurn (staged:1664) gates the push on isDoorSlug with no else/telemetry/fallback (partially mitigated by the matchDoors corpus backstop at :1191).
- **Reproduction:** /tmp/sweep-capture/probe3.ts (note_door('acceptance') accepted by parse but absent from description; note_door('divorce')+note_door('empty_nest') → ['empty_nest'])
- **Evidence:** onboarding-staged.ts:1580-1581,1664,1191,1697,1700; doors.ts:11-24,23,30-32; repro /tmp/sweep-capture/probe3.ts

### [CAT-12] Uppercased/decorated identity-skip sentinel is committed as a literal handle ('the __identity_skip__')
- **Severity/Confidence:** data · reproduced
- **Class:** `brittle-magic-string-sentinel`
- **Surface:** identityPickIsSkip exact-match sentinel (onboarding-staged.ts:177-182)
- **Symptom:** '__IDENTITY_SKIP__' (uppercase) and '__identity_skip__ please' are NOT recognized as the skip sentinel and are committed as the identity noun and advanced to gap, so the rendered handle becomes 'the __identity_skip__.'
- **Root cause:** identityPickIsSkip matches only via case-sensitive `t === IDENTITY_PICK_SKIP` (trimmed but not case-folded, no substring), and the SKIP_RE fallback requires >=2 words so a single mangled token misses it. Protected in practice only because the button submits the exact lowercase constant — a latent trap for any paste/altered-casing/future caller, and a symptom of a brittle exact-match UI↔engine contract that degrades to a garbage capture.
- **Reproduction:** /tmp/sweep-idpick/probe.ts §8 (pick '__IDENTITY_SKIP__' → noun='__identity_skip__', stage=gap)
- **Evidence:** onboarding-staged.ts:171,177-182; identity-picker.tsx:10,63; repro /tmp/sweep-idpick/probe.ts §8

### [CAT-13] The deterministic completion contract (contractMet/contractGaps) is DEAD on the shipping staged path — members reach 'complete' with the contract unmet and commit
- **Severity/Confidence:** data · reproduced · merged 3 dupes
- **Class:** `contract-enforced-only-off-the-live-path`
- **Surface:** Onboarding staged finalize (applyStagedTurn→grinta onComplete→card) + finalizeOnboardingAction/runOnboarding
- **Symptom:** A member reaches stage:'complete' (→ working 'This is me' commit) while the onboarding completion contract is UNMET, and the card renders identically because chat.tsx builds it from collected without reading card.ready/card.missing. Reproduced sub-cases that commit: empty reclaim list, 1-item list, skipped identity + no athleticPast, and a bare future-goal gap ('I'd like to lose 30 lbs') in the Fade-story slot. finalize/runOnboarding never re-derives contractMet server-side — a 1-char junk gap ('x') also persists {ok:true}.
- **Root cause:** The staged path reaches completion by stage-machine progression (reclaimStage.gather→enterGrintaSurvey→grintaStage.onComplete sets complete=true) and never invokes contractMet/contractGaps, which are wired only to the dormant v1 engine (onboarding.ts:513,1072). Commit-time validation (flow.ts:53-64) checks only name/email/identity-or-skip + validateReconnectOutput (reclaim floor + doors) and never validates athleticPast presence or gapIsNarrative — those live ONLY in the dead contractGaps. The strongest gate is unreachable code on prod; the persist boundary trusts 'complete' instead of re-deriving.
- **Reproduction:** /tmp/sweep-cardgate/probe3.ts (PATH3 no athleticPast, PATH5 goal-as-gap both finalize errors []=COMMIT); junk-gap 'x' → runOnboarding ok:true, contractMet=false
- **Evidence:** onboarding-staged.ts:1251-1254,1319-1326,1332-1351; onboarding-contract.ts:37-64; onboarding.ts:513,626-629,1072; flow.ts:47-64; actions.ts:137-155,229; chat.tsx:158,206,358; repro /tmp/sweep-cardgate/probe.ts,probe2.ts,probe3.ts + completion-persistence-audit script

### [CAT-14] RECLAIM_LIST_MIN (≥3) is silently downgraded to a floor-1 finalize and the reclaim stage advances on an empty list — members commit onboarding with 0–1 Reclaim items
- **Severity/Confidence:** data · reproduced · merged 3 dupes
- **Class:** `trust-the-client-floor`
- **Surface:** reclaim-list-builder.tsx + reclaimStage.gather → enterGrintaSurvey (onboarding-staged.ts:1251-1254) + flow.ts finalize
- **Symptom:** A member adds ONE item, clicks 'This is my list →' (enabled at 1), completes Grinta, and commits a 1-item Reclaim List — or, on any non-builder path (resume/replay/blank submission), the engine stores ZERO items and hands off to Grinta, dead-ending at a raw 'reclaim list must have at least 1 item (got 0)' error only at the finish line. The Reclaim List is a frozen required Reconnect output (≥3, soft-aim 7).
- **Root cause:** Two gates disagree and the strict one never runs. The builder floor is client-side `items.length>=1` (reclaim-list-builder.tsx:29), computed before server-side appendReclaim filtering. reclaimStage.gather calls enterGrintaSurvey UNCONDITIONALLY with no length/floor check. Commit validation uses RECLAIM_LIST_FLOOR=1 (reclaim.ts:20,111), and MIN=3 is enforced only in the dead contractGaps (onboarding-contract.ts:54). The engine trusts the client to own a floor it should enforce at the enterGrintaSurvey chokepoint.
- **Reproduction:** /tmp/sweep-cardgate/probe3.ts PATH2 (1-item list COMMITS); /tmp/sweep-terse-frontloader/contract-bypass.ts (empty submission → grinta → complete)
- **Evidence:** reclaim-list-builder.tsx:29-30,72; onboarding-staged.ts:1251-1254,740-745; reclaim.ts:19-21,107-113; flow.ts:62; onboarding-contract.ts:54; repro /tmp/sweep-cardgate/probe.ts PATH1/PATH2, probe3.ts, /tmp/sweep-terse-frontloader/contract-bypass.ts

### [CAT-15] Structured Reclaim builder routes verbatim entries through the fuzzy conversational appendReclaim — deliberate member items are silently dropped, folded, or emptied below the floor
- **Severity/Confidence:** data · reproduced · merged 2 dupes
- **Class:** `deterministic-surface-through-fuzzy-primitive`
- **Surface:** Onboarding reclaim stage — reclaimStage.gather → appendReclaim (onboarding-staged.ts:1251-1253,665-698)
- **Symptom:** The builder promises '100% verbatim, nothing dropped,' but the engine silently mutates deliberate entries: ['Run','Run a marathon']→['Run a marathon']; ['See my kids','See my kids more often']→drops the shorter; ['Play golf on weekends','golf']→drops 'golf'; ['Finish the deck','Done']→drops 'Done' (assent guard); ['Meditate','Every morning']→['Meditate, Every morning'] (cadence-fold fabricates an item); ['yes','done']→[] then advances to Grinta with an empty list; subset chains yield below-min lists still advancing.
- **Root cause:** reclaimStage.gather funnels every parsed entry through appendReclaim (665-698), which applies fuzzy guards built for the OLD lossy extraction: isProcessMetaOrAssent/affirmsReflection rejection (670), exact-token dedup (674), bare-cadence Rule-4 fold (675-681, BARE_MODIFIER_RE 491-492), and bidirectional token-subset drop/replace (684-693). parseReclaimListSubmission itself is clean; the loss is entirely in appendReclaim. The structured path needs a verbatim append (exact case-insensitive dedup only) and a post-filter floor re-check, not the conversational primitive.
- **Reproduction:** /tmp/sweep-reclaim/probe2.ts (drop/fold enumeration); probe3.ts (['yes','done']→[] → stage 'grinta')
- **Evidence:** onboarding-staged.ts:1251-1253,665-698,675-681,684-693,491-492,670; chat.tsx:427; reclaim-list-builder.tsx:6-8,29; reclaim.ts:19-21; repro /tmp/sweep-reclaim/probe2.ts,probe3.ts

### [CAT-16] Model add_reclaim_item / refine_reclaim_item phantoms pollute or clobber the verbatim structured list (legacy tool path left live)
- **Severity/Confidence:** data · reproduced · merged 1 dupes
- **Class:** `legacy-tool-path-not-gated-off`
- **Surface:** Onboarding reclaim stage — runArcTurn mergeStaged model.record merge (onboarding-staged.ts:1375-1393)
- **Symptom:** The Reclaim List reaching the card contains items the member never typed: the model's reworded/invented add_reclaim_item calls land alongside the verbatim submission (3 builder items + 3 phantoms = 5). refine_reclaim_item always clobbers list[last], destroying an unrelated want (front-loaded 'ride my bike'+'lose weight', refine to 'ride…twice a week' → two bike items, 'lose weight' destroyed).
- **Root cause:** reclaimStage.gather uses parseReclaimListSubmission and ignores model.record, but runArcTurn calls mergeStaged with model.record unconditionally (1375) and refine sets list[last]=refineReclaim (1378-1381) assuming the last add is the physical last element — false when adds accumulate across turns or appendReclaim silently dedups/folds. stageInstruction(reclaim) (1790-1822) still solicits add/refine tools even though the surface moved to deterministic capture. The refine target is inferred by position instead of tracked.
- **Reproduction:** /tmp/sweep-capture/probe2.ts (3 phantoms merged); probe.ts P1/P2 (refine clobbers 'lose weight'/'see my friends')
- **Evidence:** onboarding-staged.ts:1375-1393,1251-1253,1802-1812,665-698,787; onboarding.ts:626-629; chat.tsx:427; repro /tmp/sweep-capture/probe2.ts,probe.ts

### [CAT-17] reclaimCategories desyncs from reclaimList when the seed category array is shorter — IDQ-dimension data mispairs positionally
- **Severity/Confidence:** data · reproduced
- **Class:** `parallel-array-invariant-unenforced`
- **Surface:** Onboarding reclaim capture — appendReclaim category bookkeeping (onboarding-staged.ts:696) + consolidateReclaim
- **Symptom:** reclaimCategories becomes length-mismatched with reclaimList (list len 3–4, cats len 2); consolidateReclaim pairs categories positionally, so IDQ dimensions attach to the wrong items or new items get none.
- **Root cause:** appendReclaim rebuilds cats from the existing array and only appends a slot per new item, never back-filling slots for pre-existing category-less items, so a shorter incoming array stays offset — two index-locked arrays maintained independently with no invariant at the append site.
- **Reproduction:** /tmp/sweep-capture/probe5.ts case C (seed cats len 2, append → list len 3–4, cats len 2)
- **Evidence:** onboarding-staged.ts:696,787; reclaim.ts:69-98; repro /tmp/sweep-capture/probe5.ts case C, probe6.ts

### [CAT-18] A bulleted/list block at the gap 'is there more?' confirm is swallowed raw into the gap prose — the items never reach the Reclaim List
- **Severity/Confidence:** data · reproduced
- **Class:** `structured-input-into-freetext-gate`
- **Surface:** Onboarding gap stage confirm — resolveGapConfirm/memberAddingMoreGap (onboarding-intent.ts:101-111,199-204)
- **Symptom:** A member pasting a bulleted list at the gap reflect-confirm (their reclaim wants, or fade chapters) has it classified as an 'addition' and appended bullets-and-all into collected.gap ('…what to do with myself. • Rediscover what I enjoy'), corrupting the stored gap narrative; those items never become Reclaim List entries.
- **Root cause:** memberAddingMoreGap treats any >=25-char, >=4-content-word non-confirmation reply as fresh gap material; a multi-line bulleted block clears that bar and the 'addition' branch (staged:1217-1221) joins the raw message. No guard recognizes a structured list block at the gap confirm — the same 'a list is not prose' shape parseReclaimListSubmission solved for the reclaim stage, unguarded here.
- **Reproduction:** /tmp/sweep-singleevent/probe4.ts (resolveGapConfirm of a 3-bullet block => 'addition', bullets embedded in gap)
- **Evidence:** onboarding-intent.ts:101-111,199-204; onboarding-staged.ts:1217-1221; repro /tmp/sweep-singleevent/probe4.ts + probe.ts Persona 3/4

### [CAT-25] tidyGapProse mangles the member's own gap words — abbreviations broken ('3 p.m.'→'3 p. M.', 'e.g.'→'e. G.') despite promising no word changes
- **Severity/Confidence:** data · reproduced
- **Class:** `naive-regex-on-member-voice`
- **Surface:** Onboarding gap capture — tidyGapProse (onboarding-staged.ts:416-423)
- **Symptom:** The member's fade story is shown back with broken abbreviations and spurious capitalization, distancing them from their own account, though the function comment promises it changes no words.
- **Root cause:** tidyGapProse treats every period-then-letter as a sentence boundary (space + capitalize, 420-421) with no abbreviation guard; applied at :1663 and the backstops :1155,:1160,:1167,:1175.
- **Reproduction:** /tmp/sweep-capture/probe7.ts ('3 p.m.'→'3 p. M.'; 'e.g.'→'e. G.')
- **Evidence:** onboarding-staged.ts:413,416-423,1155,1160,1167,1175,1663; repro /tmp/sweep-capture/probe7.ts

### [CAT-30] Drift→Window handoff leaves awaitingConfirm=true — the Window vision beat is skipped, the spark keeper is empty, and Tuesday-vision turns are mis-scored as Checkpoint grit
- **Severity/Confidence:** data · reproduced
- **Class:** `state-flag-leak-across-transition`
- **Surface:** Reconnect arc — driftStage.confirm 'done' branch (lib/agent/reconnect.ts:454-463)
- **Symptom:** After the member confirms the Drift reflection, the arc advances to Window with awaitingConfirm still true, so their FIRST Window turn (their real reclaimed-Tuesday vision) is dispatched to windowStage.confirm() not gather(). If it classifies as 'done', the Window beat is skipped in one turn: the 'spark'/'window' keeper is queued with payloadRef NONE (the vision silently dropped) and the arc jumps to Checkpoint, where subsequent Tuesday-description turns are fed to parseLikert as grit answers ('waking at 5 and running 3 miles' records 5 as a grit score) — so the ceremony's 'you moved the Index' is computed from garbage.
- **Root cause:** driftStage.confirm's 'done' branch sets b.stage='window' but never sets b.awaitingConfirm=false, unlike its sibling dispute/addition branches (449,452); beatState() persists awaitingConfirm:true. Window is the only draw-out stage a draw-out confirm hands into (doorsStage/windowStage hand into administered stages where the kernel force-clears the flag at 1430), so only drift→window is exposed — 'a stage transition must set the destination's dispatch flag explicitly' is honored only by accident elsewhere.
- **Reproduction:** /tmp/sweep-reconnect/probe9.ts (first window vision with 'done' → checkpoint, keeper payloadRef NONE); probe14.ts (grit=[5] from morning description)
- **Evidence:** reconnect.ts:454-463,449,452,521-525; onboarding-staged.ts:1429-1430,1016; repro /tmp/sweep-reconnect/probe8b.ts,probe9.ts,probe13.ts,probe14.ts

### [CAT-33] parseLikert records the FIRST in-range number in a free-text answer, mis-scoring validated instruments (SDT/self-management/Grinta) when the rating isn't leading
- **Severity/Confidence:** data · reproduced
- **Class:** `greedy-first-match-numeric-parse`
- **Surface:** Shared administered kernel — parseLikert (onboarding-staged.ts:947-956), reachable via free-text error-recovery/resume
- **Symptom:** When an administered item (B1 SDT 1–7, B2 1–4, R4/B4 Grinta 1–5) is answered as free text, parseLikert grabs the first 1–2 digit number not the intended rating: 'on day 3 I feel a 5'→3; 'I have 2 kids but honestly 4'→2; 'like a 2, maybe a 3'→2; 'maybe a 4 or 5'→4. Wrong values feed validated construct scores.
- **Root cause:** parseLikert uses /\b(\d{1,2})\b/ and takes match[1] — the first number anywhere — with no 'exactly one number'/'answer is only a number' or ambiguity guard, so a leading incidental number beats the intended trailing rating. Exposure is narrow (chat surfaces render ScaleChips and hide the box; free text reaches parseLikert only on error-recovery/resume, and the scale-chips.tsx:10 'text box stays available' comment is stale).
- **Reproduction:** /tmp/sweep-rrarc/likert.ts (parseLikert('on day 3 I feel a 5',5)=3; 'I have 2 kids but honestly 4'=2)
- **Evidence:** onboarding-staged.ts:947-956,983; rebuild-chat.tsx:74,115,122; chat.tsx:431; scale-chips.tsx:10; repro /tmp/sweep-rrarc/likert.ts

### [CAT-34] Leading-affirmation-word promoted to a full CONFIRM across every coach-mode propose-confirm point (B3, C1, C3, W1, W3) — 'yes but…' commits the un-tweaked artifact and drops the member's change
- **Severity/Confidence:** data · reproduced · merged 3 dupes
- **Class:** `leading-affirmation-as-whole-confirm`
- **Surface:** Coach/confirm regexes — rebuild.ts (pilotConfirms), reclaim.ts (refineConfirms/c3Confirms), rewire.ts (memberClosingAffirm, W3_CONFIRM_OFFER_RE)
- **Symptom:** After a plan/list/line is proposed, a revision opening with an affirmation token is misread as a whole confirm and the member's requested change is silently dropped: B3 'yes but make the walk shorter'/'perfect except swap eating for fruit' → plan locks as-is (and a same-turn model plan field silently overwrites the committed value); Reclaim C1/C3 'perfect except the last one'/'looks good but move kids to top' → list committed unchanged; Rewire W1 a first true line containing 'perfect'/'good enough'/"I'm done" is dropped as a close (line lost, member just nudged); Rewire W3 'Yes, I can always begin again' stores the OLD line instead of the member's new bad-day line.
- **Root cause:** The confirm predicates are ^-anchored word-boundary alternations that inspect only the leading token (PILOT_CONFIRM_RE rebuild.ts:215-216; REFINE_CONFIRM_RE/c3 reclaim.ts:96-100,382-386; W3_CONFIRM_OFFER_RE rewire.ts:633-640; RECLAIM_CLOSE_RE unanchored, onboarding-intent.ts:73) with no negative lookahead for a 'but/except/though/actually' revision tail; coach-mode also ingests model.plan fields at the top of the handler before the confirm check. Same 'guess promoted to committed truth' pattern onboarding fixed in 0696d7b — a close/affirm must be a WHOLE-message intent that rejects a substantive tail. Blast radius: all coach-mode arcs.
- **Reproduction:** /tmp/sweep-rrarc/b3b.ts (six 'yes but' revisions all complete=true); /tmp/sweep-reclaim/probe3.ts (four tweak phrases all COMMIT); /tmp/sweep-rrarc/w3.ts ('Yes, I can always begin again' stores old line)
- **Evidence:** rebuild.ts:215-219,231-266; reclaim.ts:96-100,128-133,382-386; rewire.ts:104-108,159-171,633-640,726-740; onboarding-intent.ts:73-77; actions.ts:206-224 (durable-commitment/Playbook write); repro /tmp/sweep-rrarc/b3b.ts,b3.ts,w1b.ts,w3.ts, /tmp/sweep-reclaim/probe2.ts,probe3.ts,probe4.ts

### [CAT-36] C1 refinement dead states / no-op commits: empty-top3 stalls forever, and a NO-OP against drifted DB or model-hallucinated originals commits nothing while telling the member the list was updated
- **Severity/Confidence:** data · reproduced · merged 2 dupes
- **Class:** `unverified-model-record-vs-ground-truth`
- **Surface:** Reclaim C1 Step 2 refine — reclaim.ts refineStage.coach + refinement-store.ts commitRefinement/matchId
- **Symptom:** Three related C1 breaks: (a) valid items but empty top3 → ready predicate never fires, sc.proposed never set, the confirm branch is unreachable, every later 'yes, save it' re-emits model text (stall, no forceProgress); (b) when reclaim_item is empty/column-drifted but member_profile.reclaim_list JSONB holds the real list, the seed reads JSONB but commitRefinement matches only reclaim_item rows → applied=0, JSONB untouched, yet the member is told the list now reflects them; (c) when the model records originals matching no live item (invents wants or passes refined text as original), the engine proposes and commits a list that isn't theirs → applied=0, nothing written.
- **Root cause:** ready=items>0 && top3>0 treats top3 as a hard precondition though sanitizeRefinement lets empty-top3 through, creating a dead state with no path to complete/reject; the read side (liveReclaimTexts) has a JSONB drift fallback but the write side (commitRefinement/getReclaimItems) does not (source-of-truth asymmetry); and sanitizeRefinement validates only types+tier, never that each original traces to a real seeded reclaimList entry. In all cases the completion reply (reclaim.ts:132) is emitted regardless and the commit outcome is swallowed (actions.ts:190-196).
- **Reproduction:** /tmp/sweep-reclaim/probe.ts A (empty top3 → complete=false, stall); C (hallucinated originals → applied=0, complete=true)
- **Evidence:** reclaim.ts:102-110,123-125,132,141-145,400; refinement-store.ts:34-35,51-55,63-65; store.ts:52-66; actions.ts:56-61,190-196; repro /tmp/sweep-reclaim/probe.ts A/B/C,probe6.ts H

### [CAT-39] Degrade-to-minimal omits `today`, so the 'NEVER GUESS THE DATE' instruction fires with no date — risks a mis-dated Reclaim item/milestone/measure
- **Severity/Confidence:** data · reproduced
- **Class:** `fallback-drifted-from-prompt-invariant`
- **Surface:** Dashboard Companion — minimal fallback (checkin-actions.ts:79-90 vs :206)
- **Symptom:** The minimal context omits `today`, so contextBlock renders no 'Today is …' line, but the prompt asserts one is always present and must never be guessed. On a degrade, if the member asks to date/refine a Reclaim item, milestone, or measure this turn, the model falls back on a guessed (training-cutoff) year — the exact wrong-date-in-record failure the instruction prevents.
- **Root cause:** `today` is set only on the full-context return (206), not backfilled into minimal (79-90), even though it depends on no DB read and is free to always supply — the fallback object drifted out of sync with a prompt invariant.
- **Reproduction:** grep: `today` at 62/65 (helpers) and 206 (full path) only, never in minimal; contextBlock renders the date line only when c.today
- **Evidence:** checkin-actions.ts:79-90,206,62,65; checkin.ts:206,329

### [CAT-42] Curriculum registry reads REWIRE/REBUILD/RECLAIM/REDESIGN at module-load while every other consumer reads per-request — an env-only flip can transiently disagree with route guards
- **Severity/Confidence:** data · high
- **Class:** `inconsistent-flag-resolution-timing`
- **Surface:** lib/curriculum/registry.ts (CURRICULUM + BADGES) vs per-call flag readers
- **Symptom:** The active curriculum arms and badge set (REDESIGN_BADGES vs LEGACY_BADGES) are frozen at first import (top-level const); route guards, dashboard, badges page, and the reclaim gate re-read their flags per request. On a warm/long-lived process a flag changed without a cold start leaves the registry disagreeing with live route guards — e.g. badges page choosing redesignView() at runtime while the registry still serves LEGACY_BADGES from module init.
- **Root cause:** Two flag-resolution disciplines coexist with no convention: module-load consts in the registry (registry.ts:15,18,21,24 → CURRICULUM 109-143, BADGES 192) vs per-call functions everywhere else (redesign.ts:5-27, reclaim.ts:18-20, readiness.ts:20). So an env-only change (which RECLAIM_GATE's comment promises works 'no deploy') would not re-init the registry-baked flags — the 'toggle the env var' mental model is unsafe for these four flags.
- **Reproduction:** Code path: registry consts evaluated at module top level vs redesignEnabled()/reclaimEnabled() per call; badges page picks view at runtime but reads module-load BADGES
- **Evidence:** registry.ts:15,18,21,24,109-143,192; redesign.ts:5-27; readiness.ts:17-20; badges/[memberId]/page.tsx:21-24

### [CAT-45] Summary card renders an empty 'what you want back' list with a bare heading (no dash fallback) when the Reclaim List is empty
- **Severity/Confidence:** data · high
- **Class:** `inconsistent-empty-state-render`
- **Surface:** Onboarding summary card reclaim block (chat.tsx:380-385) + buildSummaryCard
- **Symptom:** When the card is reached with an empty Reclaim List (per CAT-13/CAT-14), the 'Your comeback — what you want back' section renders a heading over an EMPTY <ul> — a dangling label with nothing under it — while the Doors row directly above handles emptiness with a '—' fallback, so the card looks half-broken rather than merely sparse.
- **Root cause:** chat.tsx:382-384 maps card.reclaimList into <li> with no length guard/fallback, unlike the Doors row (370-378) which does `card.doors.length ? <ul> : '—'`; buildSummaryCard passes consolidateReclaimList(c.reclaimList ?? []) straight through, so an empty input yields an empty array with no placeholder. Inconsistent empty-state handling + trusting an upstream invariant the contract can bypass (the UI blast-radius of the empty-list bypass).
- **Reproduction:** Code: reclaim row maps with no length guard vs doors row '—' fallback; empty reclaimList reachable per CAT-13
- **Evidence:** chat.tsx:380-385,370-378; onboarding-contract.ts:90

### [CAT-47] Reclaim List dashboard flank has no empty-state — renders a blank card when all items are released (subpage guards it, flank does not)
- **Severity/Confidence:** data · reproduced
- **Class:** `inconsistent-empty-state-render`
- **Surface:** Triptych right flank — triptych-right.tsx:90-99 vs reclaim-list subpage
- **Symptom:** A member whose active Reclaim List is empty (every item released to 'No Longer Central' during C1 refinement, or an interrupted/no-fade path reaching the dashboard with zero rows) sees a Reclaim List card with title, subtitle, and 'See more →' but a completely blank body — it reads as broken/lost rather than not-yet-started.
- **Root cause:** triptych-right.tsx maps dash.reclaimItems into <ul> with no length check, while flow.ts:245 filters out released items (so releasing all yields reclaimItems=[]); the subpage guards this with 'Your list lands here…' copy but the compact flank read was moved as-is without mirroring the guard — an asymmetry with the sibling detail page on member data that can legitimately be empty.
- **Reproduction:** /tmp/sweep-triptych/probe.mjs (empty map → '<ul class=r-reclaim-list></ul>' with no fallback)
- **Evidence:** triptych-right.tsx:90-99; reclaim-list/[memberId]/page.tsx:36-37; flow.ts:242-246; repro /tmp/sweep-triptych/probe.mjs

---

## Functional

### [CAT-05] Ambition false-positive wipe/stall traps a real accumulation-fade member in the gap stage indefinitely with gap and Doors deleted each turn
- **Severity/Confidence:** functional · reproduced · merged 1 dupes
- **Class:** `asymmetric-backstop`
- **Surface:** Onboarding gap stage — FADE GATE isForwardAmbition wipe (onboarding-staged.ts:1150) + blocked escapes
- **Symptom:** An upbeat accumulation gap the MODEL tags (set_gap) is wiped every turn because it contains an ambition word ('bigger job'); the member is neither declined nor advanced, re-answering 'how the gap opened' indefinitely (25-turn walk stays in gap, collected.gap undefined). The same false positive also occurs when the engine correctly clears a fabricated optimizer gap but then has no forward decline action, leaving the member stranded.
- **Root cause:** onboarding-staged.ts:1150 deletes the stored gap when isForwardAmbition(gap) is true; both escape hatches are blocked by the same false positive — GAP_MAX_TURNS backstop (1165-1167) requires !isForwardAmbition(corpus), and gapTurns barely increments because the model re-tags the gap or is only counted while !gap. The ambition-clear removes the gap but never itself triggers the decline (which needs gapTurns>=2), so a destructive validation wipe with no floor and no special case for 'the model DID tag it' closes every exit.
- **Reproduction:** /tmp/sweep-doors/probe6.ts (model set_gap turn1, neutral follow-ups → stage=gap, gap=undefined after 20 turns)
- **Evidence:** onboarding-staged.ts:1150,1162,1165-1167,1180-1181,1189; repro /tmp/sweep-doors/probe4.ts,probe6.ts,probe7.ts, /tmp/sweep-nofade/probe5.ts

### [CAT-09] Backstop Door matcher misses canonical Empty Nest phrasing ('kids left for college', 'house went quiet/silent')
- **Severity/Confidence:** functional · reproduced
- **Class:** `alias-coverage-gap`
- **Surface:** Door matcher — DOOR_ALIASES.empty_nest (lib/doors.ts:63)
- **Symptom:** An empty-nest member who describes it in the Door's own descriptor words ('the kids left for college and the house went quiet/silent') gets NO Door tagged when the model omits note_door, so the backstop cannot recover a textbook Empty Nest fade and the stored Door set is empty.
- **Root cause:** DOOR_ALIASES.empty_nest has 'house got quiet' (past 'got') but not 'went quiet'/'went silent', and 'off to college'/'last one left' but not 'left for college'/'left for school' — the alias set doesn't cover the Door's own displayName descriptor, so the exact story a member would echo isn't matched.
- **Reproduction:** matchDoors('The kids left for college and the house went quiet') => [] vs matchDoors('my kids moved out') => ['empty_nest']
- **Evidence:** doors.ts:63; repro /tmp/sweep-singleevent inline matchDoors run + probe.ts Door-matcher block

### [CAT-19] Front-loaded fade content offered in the identity stage is discarded, not parked/re-surfaced — the gap opens cold on an already-answered question
- **Severity/Confidence:** functional · reproduced · merged 1 dupes
- **Class:** `stage-boundary-content-loss`
- **Surface:** Onboarding identity→gap transition (onboarding-staged.ts:1100-1131)
- **Symptom:** A member who volunteers their fade story during the identity draw-out (or a front-loader who dumps identity+gap+wants in message 1) is either (a) re-asked the gap cold via gapBridge as if nothing was heard even though collected.gap is already populated, or (b) if terse, has that content dropped entirely and can be left with an empty gap.
- **Root cause:** identityStage.confirm (1100-1125) only checks correctsReflection and otherwise advances via gapBridge, dropping the message content; there is no cross-stage carry of a message that clearly reads as gap material (shouldCaptureStagedGap would accept it). gapStage.opener=gapBridge (1128-1131,220-228) is a fixed cold draw-out that ignores whether collected.gap is already substantive — there is no 'gap already captured → reflect it back' branch analogous to reclaimOpening's parked-item re-surface (428-437).
- **Reproduction:** /tmp/sweep-singleevent/probe6.ts (layoff story on identity-confirm turn not routed to gap); f1-trace.ts (gapBridge cold-asks despite populated gap)
- **Evidence:** onboarding-staged.ts:1100-1131,220-228,1144-1206,428-437; onboarding-intent.ts:122-128; repro /tmp/sweep-singleevent/probe6.ts, /tmp/sweep-terse-frontloader/f1-trace.ts,frontloader.ts

### [CAT-20] Static engine fallback prompts (SKIP_OFFER, gapOpen) have no rotation and the whole-reply dedup guard is defeated by the model's varying receipt / a 1-step lookback
- **Severity/Confidence:** functional · reproduced · merged 1 dupes
- **Class:** `static-prompt-plus-defeatable-dedup`
- **Surface:** Onboarding identity + gap gather — no-verbatim guard vs unrotated fallbacks (onboarding-staged.ts:1487-1490,152-154,207-214)
- **Symptom:** For a terse member the identity SKIP_OFFER body and the gap gapOpen body are emitted byte-for-byte identically across consecutive turns (reads as a broken loop), because withQuestion prepends the model's varying receipt so the whole reply differs while the engine body is identical. Even when the guard's precondition IS met it only suppresses on the immediately-following turn, so a rotating lead produces a period-2 repeat (turn 1 == turn 3).
- **Root cause:** The no-verbatim guard compares the WHOLE reply (b.reply===lastAgentReply, single-turn lookback) while withQuestion (371-383) prepends a varying receipt; unlike GAP_MORE/RECLAIM_MORE (245-257/290-302) the SKIP_OFFER/IDENTITY_REDRAW and gapOpen fallbacks have NO rotation. Fix = rotate these fallbacks and/or compare the guard on the engine body, not the whole reply.
- **Reproduction:** /tmp/sweep-terse-frontloader/repeats.ts (identical SKIP_OFFER/gapOpen bodies across turns 1/2/3; period-2 repeat at R3)
- **Evidence:** onboarding-staged.ts:1487-1490,1510-1513,371-383,152-154,207-214,245-257,290-302; repro /tmp/sweep-terse-frontloader/repeats.ts R1/R2/R3

### [CAT-21] Gap-stage dispute loops to the 30-turn hard ceiling — the shared bounce ceiling is counted only on 'addition', not 'dispute'
- **Severity/Confidence:** functional · reproduced
- **Class:** `inconsistent-anti-loop-ceiling`
- **Surface:** Onboarding gap stage confirm handler (onboarding-staged.ts:1213-1227)
- **Symptom:** A member who repeatedly disputes the gap reflection with a >=20-char message never advances; the engine oscillates gather↔confirm↔dispute↔reopen for ~30 turns until the absolute hard ceiling (30 member turns) force-advances to reclaim. The shared bounce ceiling of 4 never fires.
- **Root cause:** gapStage confirm's dispute branch (1213-1216) reopens the beat but never calls confirmBounceExceeded; the counter is incremented only in the addition branch (1227). gather keeps gapDepth at/above max so it immediately re-reflects, and the idle counter never trips because a 24-char dispute satisfies offersSubstance (length>=20). Identity's confirm counts the bounce on a dispute (1109); gap does not — the shared anti-loop ceiling is applied inconsistently across confirm handlers.
- **Reproduction:** /tmp/sweep-corrector-crisis/gap-ceiling.ts (dispute every turn → stage stays gap 30 turns, confirmBounces undefined, escapes at turn 30) vs identity escapes at turn 11
- **Evidence:** onboarding-staged.ts:1213-1216,1227,1109,870-874,1196,1132,1462,119; repro /tmp/sweep-corrector-crisis/gap-realistic.ts,gap-ceiling.ts vs identity-realistic.ts

### [CAT-27] 'Welcome back — nothing's lost' resume gate fires for a terminal declined session (over-promises resume)
- **Severity/Confidence:** functional · high
- **Class:** `coarse-liveness-resume-signal`
- **Surface:** chat.tsx mount 'resumable' effect + gate copy (chat.tsx:71-92,265-270)
- **Symptom:** The resumable effect treats ANY saved session with messages.length>0 as resumable and shows 'Welcome back… nothing's lost… Pick up where I left off →' — so a correctly-declined out-of-scope member returning is greeted as resumable and dropped into the broken resume of CAT-26. The copy lies about a terminal decline.
- **Root cause:** The 'is this resumable' signal is purely messages.length>0 (85) plus saved-email presence (74); neither the effect nor the gate inspects state.stage to exclude a terminal 'declined' session, though loadOnboardingSession exposes it — the UI twin of CAT-26's engine fall-through.
- **Reproduction:** Code path: loadOnboardingSessionAction returns declined session (messages present) → resumable stays true → 'nothing's lost' branch renders
- **Evidence:** chat.tsx:71-92,265-270,317; onboarding-session.ts:35-46

### [CAT-28] Arc ceremony-data load failure strands the member in a verbatim-repeating dead-end across all four arcs (no client fallback, session already cleared)
- **Severity/Confidence:** functional · reproduced
- **Class:** `overlay-terminal-no-fallback`
- **Surface:** app/{reconnect,rewire,rebuild,reclaim}/*-chat.tsx stage==='ceremony' branch
- **Symptom:** When an arc crosses checkpoint→ceremony the client sets stage='ceremony' then awaits the ceremony-data action; if it returns !ok (any DB/read failure) the ceremony state stays null, done is never set, and the member sits in the normal chat on the 'ceremony' stage: text box, no chips, every message returns the identical ceremony-lead line forever. Refresh doesn't help — the action already cleared the arc_session at the crossing, so a refresh restarts the whole checkpoint.
- **Root cause:** Checkpoint stages set b.stage='ceremony' WITHOUT b.complete=true (rewire.ts:918-923, rebuild.ts:410, reclaim.ts:523), so the client's done branch (only on stage==='complete') never runs; the client guards `if (c.ok && c.data) setCeremony(...)` with NO else/retry, and the ceremony StageDef gather/confirm just re-emit the same lead line (rewire.ts:934-939, reconnect.ts:704-709). Same shape in all four arc clients.
- **Reproduction:** /tmp/sweep-arcui/ceremony.ts (3 turns at stage 'ceremony' → identical reply, complete=false, no exit)
- **Evidence:** rewire-chat.tsx:90-93; reclaim-chat.tsx:88-91; rebuild-chat.tsx:91-94; reconnect-chat.tsx:77-80; onboarding-staged.ts:1429-1434; rewire.ts:929-940; reconnect.ts:699-710; rewire/actions.ts:185-186; repro /tmp/sweep-arcui/ceremony.ts

### [CAT-31] Administered IDQ/Checkpoint stages have no idle backstop or decline off-ramp — a member who won't answer with a number is stuck in an infinite reprompt
- **Severity/Confidence:** functional · reproduced
- **Class:** `missing-bounded-failure-contract`
- **Surface:** Reconnect arc — measurement + checkpoint administeredStage (onboarding-staged.ts:982-993,1429-1435)
- **Symptom:** During the IDQ or Checkpoint, any non-numeric reply ('I don't want to answer these', 'this feels like a test') makes parseLikert return null and re-prompts the SAME item forever — no idle ceiling, no hard-turn ceiling, no skip/decline path. A member can be trapped with no exit but abandoning the session (30 consecutive refusals recorded 0 responses, identical reprompt each time).
- **Root cause:** runArcTurn returns early for administered stages (1429-1435) BEFORE the idle/hard-ceiling backstop (1458-1465) is ever evaluated; administeredStage.administer (982-993) handles val==null by re-prompting the current item with no attempt counter, skip, or escape. The draw-out runaway backstop is opted out of with no replacement.
- **Reproduction:** /tmp/sweep-reconnect/probe15.ts (30 non-numeric refusals → stage stays measurement, 0 recorded, identical reprompt)
- **Evidence:** onboarding-staged.ts:1429-1435,982-993,1458-1465,947-956 (parseLikert); repro /tmp/sweep-reconnect/probe15.ts

### [CAT-32] IDQ and Grit share one administeredResponses accumulator with an out-of-band reset in a distant stage-confirm handler — a single missed reset scores IDQ items as grit
- **Severity/Confidence:** functional · high
- **Class:** `shared-mutable-accumulator-implicit-reset`
- **Surface:** Reconnect arc — administeredResponses lifecycle measurement→drift→window→checkpoint (reconnect.ts:542-543; actions.ts:88-91)
- **Symptom:** The 24 IDQ answers persist unchanged through Drift and Window and are cleared only inside windowStage.confirm just before Checkpoint. Currently correct in the happy path, but fragile: the reset lives in exactly the window-confirm branch CAT-30 can bypass, and persistCheckpoint blindly slices administeredResponses.slice(0,6) trusting the reset happened — any refactor or leaked-flag resume risks scoring the first 6 IDQ answers as grit.
- **Root cause:** The two administered instruments share the single administeredResponses field; the between-instrument reset is placed in a stage-confirm handler (reconnect.ts:543) rather than at instrument entry, measurementStage.onComplete does not reset, and persistCheckpoint (actions.ts:90) has no instrument-identity guard. A defensive design gives each instrument its own scoped response bag.
- **Reproduction:** /tmp/sweep-reconnect/probe6.ts (administeredResponses stays [3×24] through drift/window, reset only at window→checkpoint)
- **Evidence:** reconnect.ts:542-543,652-656; actions.ts:88-91; onboarding-staged.ts:989; repro /tmp/sweep-reconnect/probe6.ts,probe7.ts

### [CAT-35] B3 coach mode has no idle/runaway backstop — a member who never lands a plan is stuck in the Lifestyle Pilot indefinitely, blocking B3→B4
- **Severity/Confidence:** functional · reproduced
- **Class:** `missing-bounded-failure-contract`
- **Surface:** Rebuild B3 pilot coach mode (onboarding-staged.ts:1437-1444; rebuild.ts:224-267)
- **Symptom:** Coach stages run entirely off the depth kernel, bypassing ONBOARDING_IDLE_LIMIT/HARD_CEILING. If the model never emits both plan fields (member stonewalls, or model never calls record_plan) the stage never proposes and never completes — no forced exit, no forceProgress — so B3 loops forever and blocks the B3→B4 forecast advance (30 stonewall turns → complete=false, still 'pilot').
- **Root cause:** runArcTurn dispatches coach stages (1437-1444) before the idle/runaway machinery (1458-1465, draw-out path only), and pilotStage has no forceProgress. The plan-completeness contract guarantees 'never leave without a plan' but not 'always able to leave' — coach mode is the one kernel path with no liveness floor.
- **Reproduction:** /tmp/sweep-rrarc/b3stuck.ts (30 'I dont know' turns with model only asking → complete=false, stage 'pilot')
- **Evidence:** onboarding-staged.ts:1437-1444,1458-1465; rebuild.ts:224-267; repro /tmp/sweep-rrarc/b3stuck.ts

### [CAT-40] Half-flagged workspace: /workspace renders and emits session_open when a phase engine flag is unset, then every turn returns 'X is not enabled.'
- **Severity/Confidence:** functional · high
- **Class:** `multi-flag-surface-no-coupling-invariant`
- **Surface:** /workspace/[memberId]/[sessionKey] (page gates on REDESIGN only; phase actions gate on the phase flag)
- **Symptom:** With REDESIGN=staged but a phase flag (e.g. RECLAIM) unset, the workspace page fully renders (wayfinding/ring/artifact draw, session_open fires) but the first member message returns 'Reclaim is not enabled.' and the session cannot advance — the member sits in a live-looking session that refuses every turn, and QI sees a session_open with no matching close.
- **Root cause:** One member surface is protected by two independent, uncoupled flags at two layers: REDESIGN at the route (redesignEnabled) and the phase flag at the arc-turn action (reclaimEnabled/rewireEnabled/etc.). No assertion or shared resolver ties them; flags flip independently, so nothing prevents a deploy where the route renders while the engine behind it is dark.
- **Reproduction:** Code path: page gates only on redesignEnabled+readiness, emits session_open, renders phase chat; phase action returns {ok:false,'…not enabled.'}
- **Evidence:** app/workspace/[memberId]/[sessionKey]/page.tsx:30-50; reclaim/actions.ts:51; reclaim.ts:18-20; rewire/actions.ts:67; reconnect/actions.ts:132; workspace-session.tsx:12-15

### [CAT-41] Post-deploy smoke/greenlight can PASS on a half-flagged prod — they never exercise onboarding, phase-session, or workspace surfaces
- **Severity/Confidence:** functional · reproduced
- **Class:** `verification-coverage-gap`
- **Surface:** scripts/smoke.ts (:84-97) + scripts/greenlight.sh (:21-42)
- **Symptom:** The smoke test only logs in and checks the dashboard + read-only subpages (score/grinta/reclaim-list/movement/badges/playbook/program/field-guide); it never opens a phase session, workspace route, or the onboarding funnel. So on a half-flagged prod where a phase flag or ONBOARDING_ENGINE is accidentally unset, both smoke and greenlight report green while every session action returns '…not enabled.' — false confidence about exactly the flag-gated surfaces most likely to break (the direct check for CAT-40).
- **Root cause:** The verification tooling was built around the redesign dashboard + subpages and never extended to the conversational engine surfaces or the prospective-member funnel; greenlight only verifies the static bundle is promoted and asserts nothing about runtime flag state.
- **Reproduction:** scripts/smoke.ts:84-97 page list has no onboarding/phase/workspace routes; greenlight.sh:21-42 bundle-tells only, no flag assertion
- **Evidence:** scripts/smoke.ts:84-97; scripts/greenlight.sh:21-42

### [CAT-44] Global button:hover navy rule stomps ghost/link/secondary onboarding buttons — identity 'Not sure yet' goes invisible, 'write my own' fills navy, 'Save my place' turns destructive-red
- **Severity/Confidence:** functional · reproduced · merged 2 dupes
- **Class:** `global-button-hover-stomp`
- **Surface:** Onboarding identity picker + summary card buttons vs global button hover (globals.css:106,417-418,410,799-800)
- **Symptom:** The always-available 'Not sure yet — we'll find it later' escape hatch (a governance promise) becomes navy-on-navy invisible on hover; 'None of these — I'll write my own' gets an accidental navy fill with teal text (muddy low-contrast); and the calm 'Save my place — I'll come back' exit flips to the brand's DESTRUCTIVE Deep Red on hover, signaling danger/deletion to a vulnerable member at an exit moment.
- **Root cause:** global `button:hover{background:var(--navy)}` (globals.css:106) fills any ghost/link/secondary button that doesn't defensively re-declare `background` in its own :hover: .idp-skip:hover sets only color (which equals navy → invisible), .idp-own has no :hover at all (global wins), and .btn-secondary is semantically overloaded — its single hover rule hard-codes destructive red though it also serves benign save actions. One class of defect (global hover stomp + overloaded destructive class) across three buttons.
- **Reproduction:** /tmp/sweep-onbui/spec.mjs ('.idp-skip' color navy on navy background; '.idp-own' GLOBAL WINS navy fill); globals.css:800 (.btn-secondary:hover deep-red)
- **Evidence:** globals.css:106,417-418,410-411,799-800; identity-picker.tsx:59-65; chat.tsx:414-416; specificity proof /tmp/sweep-onbui/spec.mjs

### [CAT-46] Post-Ceremony Opening Tour on the triptych mobile fold silently drops 7 of 9 surface introductions — permanently, once per member
- **Severity/Confidence:** functional · reproduced
- **Class:** `static-measurement-vs-responsive-fold`
- **Surface:** app/dashboard/post-ceremony-tour.tsx:59-65 (visible-stop filter) on ≤1000px triptych fold
- **Symptom:** A brand-new member onboarding on a phone gets a gutted Opening Tour walking only the center-pane 'program' stop (+next-step) and never introduces ID Score, Grinta, Badges, Momentum, Community, Movement, or the Reclaim List; because the tour is marked complete + once-per-member, they never get those introductions again.
- **Root cause:** The tour computes its visible-stop list ONCE at render by filtering anchors to getBoundingClientRect().width>0; on the mobile fold the default pane is 'center' and the two non-active flanks are display:none (globals.css:2036-2038 → width 0), so all left/right-flank anchors are filtered out. The tour scrim blocks tapping the segmented control to reveal a flank mid-tour, and `stops` is never recomputed — a one-shot DOM measurement against a layout that only ever reveals one pane. Fix = reveal each stop's owning pane before measuring, or author a distinct mobile sequence.
- **Reproduction:** /tmp/sweep-triptych/tour.mjs (kept stops = ['program']+next-step; dropped idscore/grinta/badges/momentum/connect/movement/reclaim)
- **Evidence:** post-ceremony-tour.tsx:59-65,73,86; globals.css:2036-2038,995; dashboard-triptych.tsx:49; repro /tmp/sweep-triptych/tour.mjs

---

## Cosmetic

### [CAT-23] Crisis short-circuit turn drops the `expects` field, unmounting the structured picker/list-builder input
- **Severity/Confidence:** cosmetic · reproduced
- **Class:** `expects-not-rederived-from-state`
- **Surface:** Onboarding structured turns on crisis input — runArcTurn / onboardingNextTurn early return (onboarding-staged.ts:1372-1373; onboarding.ts:617-619) + chat.tsx:200
- **Symptom:** Typing crisis language into the identity write-your-own field or a reclaim item shows the 988 response correctly but the structured control disappears and is replaced by the plain text box; recovery is only via free text.
- **Root cause:** The crisis short-circuit returns no `expects` in both runArcTurn (1372-1373) and onboardingNextTurn (617-619), and chat.tsx:200 sets expects to the returned value or null unconditionally, unmounting the chooser. Because the ConvState is preserved, re-deriving expects from persisted state on a crisis turn would restore the control — the structured-turn expectation is recomputed only from the current Turn, so any early-return that omits expects drops the input.
- **Reproduction:** /tmp/sweep-corrector-crisis/crisis-state.ts (crisis on pending identity pick → crisis true, pick preserved, expects undefined)
- **Evidence:** onboarding-staged.ts:1372-1373; onboarding.ts:617-619; chat.tsx:200; repro /tmp/sweep-corrector-crisis/crisis-state.ts

### [CAT-29] Reconnect resume mid-IDQ/Checkpoint shows 'Question 1 of N' regardless of progress — the answered count is dropped (fix applied to 3 of 4 arcs, Reconnect missed)
- **Severity/Confidence:** cosmetic · reproduced · merged 1 dupes
- **Class:** `resume-drops-persisted-counter`
- **Surface:** app/reconnect/actions.ts loadReconnectSessionAction (:223)
- **Symptom:** A member resuming Reconnect mid-IDQ (e.g. item 15 of 24) or mid-grit sees the scale-chip cue reset to 'Question 1 of 24' though the correct next item and saved responses are intact — a display regression that tells them they're starting over. Rewire, Rebuild, and Reclaim don't have this bug.
- **Root cause:** loadReconnectSessionAction calls scaleExpects(RECONNECT_ARC, saved.state.stage, false) omitting the 4th `answered` arg, which defaults to 0 → index=1. The other three arc load actions pass saved.state.administeredResponses?.length ?? 0 (rewire/actions.ts:208-209, rebuild:165-166, reclaim:98-99); Reconnect was missed — copy-drift across sibling call sites sharing the helper.
- **Reproduction:** /tmp/sweep-reconnect/probe5.ts (scaleExpects without 4th arg → {index:1}; with 14 → {index:15})
- **Evidence:** reconnect/actions.ts:223; rewire/actions.ts:208-209; rebuild/actions.ts:165-166; reclaim/actions.ts:98-99; onboarding-staged.ts:1001-1007,1433; repro /tmp/sweep-arcui/probe.ts, /tmp/sweep-reconnect/probe5.ts

### [CAT-43] Onboarding chat bubbles have no overflow-wrap — a long unbroken token overflows and blows out layout (the .rmsg fix was never backported)
- **Severity/Confidence:** cosmetic · high
- **Class:** `missing-overflow-wrap-on-user-text`
- **Surface:** Onboarding conversation bubbles (.bubble) — globals.css:369; chat.tsx:327-331,427
- **Symptom:** A long unbroken string (URL, email, long compound word) typed/pasted as an onboarding reply — or as a coined identity word or reclaim item — refuses to wrap inside the token and overflows the 85% max-width bubble, forcing horizontal scroll/clipping on narrow and mobile viewports.
- **Root cause:** .bubble sets max-width:85%; white-space:pre-wrap with NO overflow-wrap/word-break; pre-wrap breaks on whitespace only. The redesign's .rmsg (globals.css:1430) added overflow-wrap:anywhere for exactly this, but the onboarding .bubble was never brought in line — a fixed-width container with no intra-word break rule.
- **Reproduction:** Code: .bubble has no overflow-wrap/word-break; .rmsg has overflow-wrap:anywhere — the fix pattern not backported
- **Evidence:** globals.css:369,1430; chat.tsx:327-331,427

### [CAT-48] Triptych member strip drops the member's Phase — phaseLabel is computed and passed but never rendered (dead prop + orphan CSS)
- **Severity/Confidence:** cosmetic · reproduced
- **Class:** `dead-prop-orphan-css`
- **Surface:** app/dashboard/dashboard-triptych.tsx (:30,42,102-112) + [memberId]/page.tsx:104-126
- **Symptom:** The member strip's stated job ('who they are + what they're reclaiming + the Phase they're in') silently omits the Phase; the member never sees which R they're on in the strip, though the page pays to compute it via a forecast lookup.
- **Root cause:** dashboard-triptych.tsx destructures and types phaseLabel but never references it in the JSX (strip renders only firstName, identitySelves, My Story); [memberId]/page.tsx computes and passes it; globals.css:2020 defines .tri-member-phase (a teal pill) that is never emitted — a feature threaded through compute→pass→type but missing at the render site.
- **Reproduction:** grep: phaseLabel referenced only in destructure+type; .tri-member-phase has zero emitters
- **Evidence:** dashboard-triptych.tsx:30,42,102-112; [memberId]/page.tsx:104-106,126; globals.css:2020

### [CAT-49] Triptych renders inside constrained global chrome on first paint — redesign-on applied only after client mount (SSR/hydration flash + composer pushed off-screen)
- **Severity/Confidence:** cosmetic · high
- **Class:** `client-effect-layout-switch-flash`
- **Surface:** app/dashboard/redesign-chrome.tsx (:10-13) + globals.css:1290-1295,2003
- **Symptom:** On first paint the triptych renders inside the global 720px column + padding with the brand-bar/footer visible, then snaps to full-bleed once JS hydrates — a brief flash of a mis-laid-out dashboard (double header, narrow column, then jump); .tri-app { height: calc(100dvh - 56px) } assumes no global chrome above it, so the composer can be pushed off-screen until the class lands.
- **Root cause:** redesign-chrome.tsx adds body.redesign-on only in a useEffect (client, post-mount), but the full-bleed layout + chrome suppression are gated on body.redesign-on (globals.css:1292-1295); during SSR/first paint the class is absent. A visual state that should be decided on the server is toggled in useEffect (pre-existing redesign pattern, made more pronounced by the triptych's fixed 100dvh math).
- **Reproduction:** Code path: body.redesign-on set in useEffect; SSR HTML lacks it, so full-bleed/chrome-suppression apply only after hydration
- **Evidence:** redesign-chrome.tsx:10-13; globals.css:1290-1295,2003

### [CAT-50] Full-bleed welcome/auth tracks use 100vw — spurious horizontal scrollbar/edge overflow on scrollbar-reserving browsers
- **Severity/Confidence:** cosmetic · medium · merged 1 dupes
- **Class:** `100vw-scrollbar-overflow`
- **Surface:** Onboarding welcome + auth hero full-bleed tracks (globals.css:1936,1981; welcome.tsx:138-143)
- **Symptom:** The welcome breaks out of the 720px main column with width:100vw; margin-left:calc(50% - 50vw). On desktop browsers that reserve a scrollbar gutter, 100vw exceeds the visible viewport width, so the page gains a spurious horizontal scrollbar / a few px of overflow at the welcome step; .auth-hero uses the same pattern.
- **Root cause:** 100vw counts the classic-scrollbar gutter; the full-bleed break-out needs width:100% on a full-width wrapper or a calc(100vw - scrollbar) guard. .onbwel-d-hero overflow:hidden clips the hero content but the outer track still contributes width.
- **Reproduction:** Code: .onbwel-track/.auth-hero width:100vw + margin-left:calc(50% - 50vw); 100vw includes scrollbar gutter
- **Evidence:** globals.css:1936,1942,1981; welcome.tsx:138-143

### [CAT-51] Grinta baseline four-R step row on the summary card can't wrap — labels risk squeeze/clip on narrow phones
- **Severity/Confidence:** cosmetic · medium
- **Class:** `no-wrap-flex-overflow-small-viewport`
- **Surface:** Onboarding summary card four-R ceremony row (.cer-journey.og-journey) — globals.css:581,473; chat.tsx:397-404
- **Symptom:** The 'Reconnect / Rewire / Rebuild / Reclaim' step row is a non-wrapping flex row capped at max-width 320px; on the narrowest phones the four ~9-char labels + three 18px gaps get squeezed together or overrun the cap without wrapping.
- **Root cause:** .cer-journey has display:flex; gap:18px with NO flex-wrap and .og-journey max-width:320px — a fixed-count, fixed-max-width flex row with no wrap for content whose intrinsic width can exceed the cap on small screens (the grinta-strands cue-wrapping fix at globals.css:278 was not applied here).
- **Reproduction:** Code: four fixed 0.75rem labels + 3×18px gaps approach/exceed 320px on <360px with no flex-wrap
- **Evidence:** globals.css:581,473,278; chat.tsx:397-404

### [CAT-52] Field Guide unconditionally promises the Strava integration while STRAVA_* is unset on prod — help copy contradicts the live control
- **Severity/Confidence:** cosmetic · high
- **Class:** `copy-feature-flag-drift`
- **Surface:** app/field-guide/[memberId]/page.tsx:59
- **Symptom:** The Field Guide tells every member 'Connect Strava and your rides, runs, and workouts flow in on their own,' but on prod STRAVA_* is unset so the actual affordance renders 'Strava connection isn't available yet.' — the guide promises a feature the dashboard says is unavailable, an inconsistency a Charter member will notice.
- **Root cause:** The Strava affordance is config-gated on stravaConfigured() everywhere it acts (dashboard, account), but the descriptive Field Guide copy is static and not gated on the STRAVA_* env — static member-facing documentation asserting a capability a flag currently disables.
- **Reproduction:** Code: field-guide:59 renders the Strava promise unconditionally; strava-connect.tsx hides/disables when !stravaConfigured()
- **Evidence:** field-guide/[memberId]/page.tsx:59; account/strava-connect.tsx:22-25; strava.ts:37-39; redesign-dashboard.tsx:296

### [CAT-53] /rhythm elicitation page is unreachable on prod — gated on MOBILE (removed) AND OUTREACH (on), silently disabling a live-flag sub-feature
- **Severity/Confidence:** cosmetic · high
- **Class:** `dead-surface-flag-coupling`
- **Surface:** app/rhythm/[memberId]/page.tsx:18
- **Symptom:** With MOBILE unset (per the 2026-07-21 mobile-dark decision) but OUTREACH on, /rhythm always redirects to /dashboard — the rhythm-elicitation UI is built and wired but dead on prod, and its only linker (the Reconnect ceremony) only pushes there when mobile, so nothing reaches it.
- **Root cause:** The rhythm page couples two flags with AND (mobileEnabled() && outreachEnabled()); when MOBILE was pulled from prod, this OUTREACH sub-feature went dark as a side effect, with no flag or note reflecting that the rhythm-elicitation half of OUTREACH is inert.
- **Reproduction:** Code: page redirects unless MOBILE && OUTREACH; sole caller pushes to /rhythm only when mobile
- **Evidence:** rhythm/[memberId]/page.tsx:18; redesign.ts:11-13; reconnect-ceremony.tsx:32-33


---

## Fix classes (fix the pattern, not the instance)

### `proxy-vocabulary-scope-gate`  — CAT-01, CAT-03
The decline/admit decision must key on COMMITTED STATE (collected.doors, the 8-Door taxonomy, a member-declared no-fade) — not on a re-parse of raw text through a narrow loss-verb allow-list or an ambition keyword. Decline on the ABSENCE OF LOSS (no committed Door AND no loss signal AND explicit no-fade), never on the PRESENCE OF AMBITION. Move augmentDoors BEFORE the decline check so the gate can read collected.doors, and make hasGenuineLoss consult matchDoors. This kills the over-decline (real Door-fade) and the over-admit (fabricated fade) simultaneously, since both currently share the same impoverished signal set.

### `model-engine-contract-drift`  — CAT-02
Give the model a DISTINCT structured signal per the arc-reliability posture: replace the single note_no_fade boolean with a three-way scope tag (e.g. scope='thriving' | 'acceptance' | 'fade') or add note_acceptance, and rewrite the tool description to check for an accumulated-Door / identity-distance fade FIRST rather than firing on 'want more'. The engine then disposes on the model's judgment instead of re-classifying text. Update the STAGED_SYSTEM prompt to carve out resignation as served, not declined.

### `asymmetric-backstop`  — CAT-03, CAT-05
Make the never-strand invariant 'capturing a fade REQUIRES a fade' rather than 'always capture something': gate the GAP_MAX_TURNS corpus grab on the same isRealFade && hasLossSignal (or committed Door) floor that shouldCaptureStagedGap uses, so a thriving corpus can't fabricate a gap. Add a symmetric DECLINE backstop so absence-of-fade at the ceiling routes to the served-Acceptance/decline fork instead of admit. Replace the destructive isForwardAmbition gap-wipe with a non-destructive re-confirm that has a floor and a special case for 'the model DID tag it', and give the wipe a forward decline action.

### `sticky-flag-not-reconciled`  — CAT-04
A boolean set from a single fuzzy model judgement must be reconciled against later evidence: re-evaluate s.noFade every turn against accumulated loss signal / committed Doors and clear it when contradicted, or make the backstop capture paths independent of noFade. Never let one early model guess latch and disable the safety net for the rest of the conversation.

### `cross-stage-corpus-contamination`  — CAT-06
Scope the Door-detection corpus to gap-stage messages only (exclude identity-stage past-self answers and captured reclaimList text), and/or add a polarity/context boundary to matchDoors so a POSITIVE past-self description or a forward WANT does not read as a fade event. Decouple Door accumulation from a non-wiped gap so a declined/stalled member's named Doors still persist per the Reconnect contract.

### `deterministic-heuristic-demotes-committed-data`  — CAT-07
correctDoors must never delete a Door the model explicitly tagged via note_door — only prune a SIBLING the model did NOT tag, and scope the disambiguation to the accumulated full gap corpus, not a per-turn slice. A keyword regex is a guess; it must never demote committed truth. Fix the enshrining test (reclaim.test.ts:130) so the over-reach is guarded.

### `enum-slug-drift`  — CAT-08
Derive the note_door tool-description slug list programmatically from the source-of-truth DOORS array so it can't drift (this restores 'acceptance'). On an invalid model slug, log/telemeter and attempt a nearest-match rather than silently dropping, so a mis-tag is recoverable and observable.

### `alias-coverage-gap`  — CAT-09
Extend DOOR_ALIASES to cover each Door's own displayName descriptor phrasing (empty_nest: 'went quiet'/'went silent'/'left for college'/'left for school'). More durably, seed aliases from the descriptor text so the backstop matcher always covers the exact story a member would echo back.

### `raw-input-promoted-to-committed-truth`  — CAT-10
Add a validity gate for any coined free-text handle that becomes a rendered label: trim trailing punctuation/quotes/emoji, cap length to a short noun phrase, reject bare articles and full sentences, and re-prompt on failure instead of blind commit. Same discipline the onboarding hardening applied — a raw string must pass a shape contract before it is promoted to committed, member-facing truth.

### `no-mutual-exclusion-contract`  — CAT-11
Enforce an arbitration contract at the gather branch: an offer_identity_words this turn SUPPRESSES a same-turn name_identity (present the chips, never commit the name unconfirmed), and a fresh model re-offer can supersede a pending pick. Make the two tools mutually exclusive in parseStagedTurn. This upholds the hard rule 'never names an identity label without member confirmation.'

### `brittle-magic-string-sentinel`  — CAT-12
Normalize the skip sentinel comparison (case-fold + trim, tolerate trailing text) or replace the magic-string UI↔engine contract with a structured skip signal that can't degrade to a garbage capture when casing/whitespace is altered by a paste or a future caller.

### `contract-enforced-only-off-the-live-path`  — CAT-13
Re-wire contractMet/contractGaps into the SHIPPING staged path: gate enterGrintaSurvey and the grinta onComplete on contractGaps (athleticPast present, identity, reclaimList≥MIN, gapIsNarrative), and re-derive contractMet server-side in finalizeOnboardingAction/runOnboarding at the persist boundary — never trust the transient 'complete' flag or a client gate. Have chat.tsx read card.ready/card.missing before showing the commit button. Kill the dead-code path so the strongest gate isn't unreachable.

### `trust-the-client-floor`  — CAT-14
Own the Reclaim floor in the engine at the enterGrintaSurvey chokepoint: after appendReclaim filtering, re-check RECLAIM_LIST_FLOOR/MIN and re-prompt in-stage rather than advancing on an empty/short list. Unify the three disagreeing floors (client builder=1, commit FLOOR=1, contract MIN=3) onto a single enforced value so the strict floor actually runs on the live path.

### `deterministic-surface-through-fuzzy-primitive`  — CAT-15
Give the structured builder its own verbatim append (dedup on EXACT case-insensitive string only — no subset drop, no cadence-fold, no assent rejection) instead of routing through the conversational appendReclaim, and re-validate the floor after the append. A deterministic surface promising verbatim fidelity must not run its input through a chokepoint built for fuzzy free-text.

### `legacy-tool-path-not-gated-off`  — CAT-16
Gate off the model add_reclaim_item/refine tool merge and the soliciting stageInstruction for the reclaim stage now that capture is deterministic — mergeStaged must not merge model.record on the structured builder turn. If refine is kept elsewhere, track the refine target by id, never by list[last] positional assumption.

### `parallel-array-invariant-unenforced`  — CAT-17
Enforce the reclaimList↔reclaimCategories index-lock at the append site: back-fill category slots for pre-existing category-less items so the arrays are always equal length, or replace the two parallel arrays with a single list of {text, category} objects to make the invariant structural.

### `structured-input-into-freetext-gate`  — CAT-18
Add a 'a list is not prose' guard at the gap confirm (reuse the parseReclaimListSubmission list-block detector): a bulleted/multi-line block at 'is there more?' should not be joined raw into the gap narrative — route it to correction/Reclaim capture or ask which it is, rather than embedding bullet literals as the stored gap.

### `stage-boundary-content-loss`  — CAT-19
Mirror the reclaim stage's park-and-re-surface for the gap: carry a member message that clearly reads as gap material across the identity→gap boundary, and give gapStage.opener a 'gap already captured → reflect it back / confirm it' branch analogous to reclaimOpening (428-437) so a front-loader is never re-asked cold and terse content isn't dropped.

### `static-prompt-plus-defeatable-dedup`  — CAT-20
Rotate the SKIP_OFFER/IDENTITY_REDRAW and cold gapOpen fallbacks the way GAP_MORE/RECLAIM_MORE already rotate, and compare the no-verbatim guard on the ENGINE BODY (not the whole reply that a varying receipt prefix defeats) with a lookback long enough to catch a period-2 repeat. Rotation makes the 1-back lead patch unnecessary.

### `inconsistent-anti-loop-ceiling`  — CAT-21
Apply the shared confirmBounceExceeded ceiling consistently across ALL confirm handlers — count the bounce on a dispute in gapStage.confirm as identity already does — so a repeated dispute hits the bounce ceiling of 4 instead of running to the 30-turn hard ceiling. Audit every confirm handler for the same omission.

### `expects-not-rederived-from-state`  — CAT-23
Re-derive `expects` from the persisted ConvState on every turn (including crisis/early-return short-circuits) rather than only from the current Turn, and have the client fall back to the last-known expects when the return omits it — so a 988 crisis response never unmounts the structured picker/list-builder.

### `governance-rule-only-in-prompt`  — CAT-24
Enforce the gap first-person governance rule in the ENGINE, not only the prompt: detect a third-person/second-person set_gap and either transform to first-person on the member's grain or reject-and-redraw, the way crisis routing and the fade gate are deterministically enforced. Never leave a hard governance constraint to the fuzzy model alone.

### `naive-regex-on-member-voice`  — CAT-25
Add an abbreviation guard to tidyGapProse's sentence-boundary regex (don't split on 'p.m.'/'e.g.'/single-letter+period) or restrict capitalization to genuine sentence starts; the function promises no word changes, so load-bearing member voice must survive verbatim.

### `unlisted-terminal-stage-fallthrough`  — CAT-26
Enumerate 'declined' (and any terminal stage) explicitly in the arc so re-entry doesn't fall through to the 'no stageDef → complete=true' branch; persist and re-hydrate the terminal flag (declined) in beatState so the Turn always carries it. Have finalize refuse to commit an empty collected. Audit for any other ConvState stage the arc doesn't list.

### `coarse-liveness-resume-signal`  — CAT-27
Gate the 'Welcome back — nothing's lost' resume on whether the saved state is actually RESUMABLE (not terminal), inspecting state.stage to exclude 'declined'/'complete', rather than on the coarse messages.length>0 liveness signal.

### `overlay-terminal-no-fallback`  — CAT-28
Add a client fallback/retry for the ceremony-data action failure in all four arc chat clients: on !ok, retry or route to a safe recovery screen rather than leaving the member in a normal chat on the 'ceremony' stage. Make the ceremony StageDef gather/confirm transition forward (or emit a recoverable state) instead of echoing the same lead line, and don't clear the arc_session until the ceremony data has loaded.

### `resume-drops-persisted-counter`  — CAT-29
Pass saved.state.administeredResponses?.length ?? 0 as the 4th scaleExpects arg in loadReconnectSessionAction to match the three sibling arcs; more durably, make scaleExpects require the answered count (no defaulting to 0) so a resume can't silently drop progress. Audit all sibling call sites when a shared helper's signature changes.

### `state-flag-leak-across-transition`  — CAT-30
Establish the invariant 'a stage transition must set the destination's dispatch flag (awaitingConfirm) explicitly' and enforce it in a single advance helper rather than per-branch, so driftStage.confirm's 'done' branch (and any draw-out→draw-out handoff) resets awaitingConfirm=false. This is the arc-reliability kernel-contract fix: the engine references STRUCTURE, transitions never improvise.

### `missing-bounded-failure-contract`  — CAT-31, CAT-35
Give every deterministic loop a bounded-failure contract: an attempt cap + a decline/skip off-ramp for administered instruments (IDQ/Checkpoint) so a member who won't answer with a number isn't trapped, and an idle/hard ceiling + forceProgress for coach-mode stages (B3 pilot) so a plan that never lands can still exit. Route the runaway/idle backstop machinery BEFORE the administered/coach early returns, not after.

### `shared-mutable-accumulator-implicit-reset`  — CAT-32
Give each administered instrument (IDQ, Grit) its own scoped response bag (like stageScratch) instead of sharing one administeredResponses field with an out-of-band reset in a distant handler; add an instrument-identity guard to persistCheckpoint so it can never slice IDQ answers as grit.

### `greedy-first-match-numeric-parse`  — CAT-33
Make parseLikert require an unambiguous single rating: prefer 'the answer is only a number' (or the last/only in-range number when a bare digit dominates), and reprompt on ambiguity (multiple numbers, incidental leading number) rather than silently recording the first match. Update the stale scale-chips.tsx:10 comment.

### `leading-affirmation-as-whole-confirm`  — CAT-34
Replace every leading-token confirm/close regex with a SHARED whole-message confirm predicate that rejects a message carrying a substantive revision/contrast tail (but/except/though/actually + change), applied uniformly across B3 pilotConfirms, Reclaim C1/C3, and Rewire W1 close / W3 reframe. Additionally, coach-mode must not ingest model plan/list fields on a proposed/confirm turn (propose-confirm-commit only). This is the systemic fix for the 'affirmation-of-frame captured as confirmation' family (cf. 0696d7b) — fix the abstraction, not each instance.

### `unverified-model-record-vs-ground-truth`  — CAT-36
In C1/C3 refinement: (a) let the propose gate fire on items alone (don't hard-require top3) and add forceProgress so an empty-top3 record can't dead-state; (b) give commitRefinement the same JSONB-drift fallback the read side (liveReclaimTexts) has, so read and write share one source of truth; (c) validate every model `original` traces to a real seeded reclaimList entry before proposing/committing, and only emit the 'your list now reflects you' reply when applied>0 — never claim an update that wrote nothing.

### `member-visible-datum-not-known-to-MA`  — CAT-37
Add a badges field to CheckinContext and read earnedBadgeIds/getPassport in buildContext, rendering it in contextBlock — closing the specific gap. Institutionalize the CLAUDE.md reconcile-with-the-MA step as part of 'done' for any member-visible datum so a dashboard feature can't ship without being made known to the always-on Companion.

### `all-or-nothing-context-under-single-trycatch`  — CAT-38
Apply per-field .catch guards to the minimal fallback the same way the full path does, so one drifted table can't collapse durable memory/story/Playbook/IDQ; and pass a 'running degraded' flag into the prompt so the Companion can honestly narrow its claims instead of confabulating under a no-memory-gap instruction.

### `fallback-drifted-from-prompt-invariant`  — CAT-39
Always set `today` (it depends on no DB read) in BOTH the full and minimal context objects, and add a check that the minimal fallback supplies every field the prompt asserts is present — so a degrade can never leave a prompt-promised invariant (the current date) unmet.

### `multi-flag-surface-no-coupling-invariant`  — CAT-40
Introduce a single shared resolver that couples the route flag (REDESIGN) with the phase engine flag for each workspace surface, and assert both at the route BEFORE rendering/emitting session_open — so the page can't render a live-looking session the engine will refuse. Don't emit session_open until the first turn can actually succeed.

### `verification-coverage-gap`  — CAT-41
Extend the smoke test to exercise the flag-gated engine surfaces (open one onboarding turn, one phase/workspace session per R) and assert a successful turn, and have greenlight assert runtime flag state — so a phase/ONBOARDING_ENGINE flag regression fails the deploy gate instead of reporting green.

### `inconsistent-flag-resolution-timing`  — CAT-42
Adopt a single flag-resolution discipline: make the curriculum registry read its flags per-request (like every other consumer) instead of at module-load, so an env change takes effect consistently and can't transiently disagree with route guards. Document which flags require a cold start until this is done.

### `missing-overflow-wrap-on-user-text`  — CAT-43
Backport the redesign's overflow-wrap:anywhere (from .rmsg) to the onboarding .bubble; adopt a shared user-text container class so any fixed-width bubble gets intra-word breaking by default and this class of overflow can't recur.

### `global-button-hover-stomp`  — CAT-44
Stop the global button:hover navy rule from stomping non-filled buttons: scope it to primary buttons (or give ghost/link/secondary variants an explicit hover background) so .idp-skip/.idp-own keep their intended appearance, and split .btn-secondary into destructive vs non-destructive variants so a benign 'Save my place' action never renders the Deep Red delete color. Add a contrast-safety check to catch same-color-on-same-color.

### `inconsistent-empty-state-render`  — CAT-45, CAT-47
Mirror the existing empty-state guards onto every panel/row that renders member-driven arrays: the summary-card reclaim row should use the same '—'/placeholder fallback the doors row has, and the dashboard Reclaim flank should reuse the subpage's 'Your list lands here…' copy. Establish that a data panel which can legitimately be empty must render a fallback, not a bare heading/blank <ul>.

### `static-measurement-vs-responsive-fold`  — CAT-46
Make the Opening Tour reveal each stop's owning pane (switch the segmented control) before measuring visibility, or author a distinct mobile tour sequence that walks all nine anchors across the folded panes — never let a one-shot width>0 filter against a single-pane fold collapse to skipping 7 of 9 once-per-member introductions.

### `dead-prop-orphan-css`  — CAT-48
Render the phaseLabel Phase pill in the member strip using the already-defined .tri-member-phase CSS (fulfilling the strip's documented purpose), or remove the computed-and-passed prop + orphan CSS if the omission is intentional. Add lint/detection for props used only in destructure+type and CSS classes with zero emitters.

### `client-effect-layout-switch-flash`  — CAT-49
Decide redesign chrome on the SERVER (set the body class / suppress global chrome at SSR time) rather than toggling body.redesign-on in a client useEffect, so the first paint is already full-bleed and the fixed-100dvh composer isn't pushed off-screen before hydration.

### `100vw-scrollbar-overflow`  — CAT-50
Replace the 100vw full-bleed break-out on .onbwel-track and .auth-hero with width:100% on a full-width wrapper (or calc(100vw - scrollbar-width)) so scrollbar-reserving browsers don't get a spurious horizontal scrollbar. Standardize one safe full-bleed helper.

### `no-wrap-flex-overflow-small-viewport`  — CAT-51
Add flex-wrap (as the grinta-strands cue fix at globals.css:278 already does) to the four-R .cer-journey step row and let it wrap on narrow phones instead of squeezing/overrunning the 320px cap.

### `copy-feature-flag-drift`  — CAT-52
Gate the Field Guide Strava copy on stravaConfigured() so the help text matches the live control; more broadly, tie member-facing documentation of any config-gated capability to the same flag the feature reads, so copy and affordance can't drift.

### `dead-surface-flag-coupling`  — CAT-53
Decouple the rhythm-elicitation sub-feature from MOBILE: gate /rhythm (and the Reconnect ceremony link) on OUTREACH alone (or its own flag) so pulling MOBILE doesn't silently kill a live-flag feature; add a note/assertion when a flag that another feature ANDs on is retired.

---

## CAT-54 — the identity pick beat can trap a member in a dead loop (FOUND 2026-07-30, walk 3, NOT FIXED)

**Severity: MEDIUM (revised down the same evening — see below). Not yet fixed; deferred to fresh eyes.**

**Severity correction.** Filed as "high", then corrected: a tapped chip is pre-vetted and bypasses the sanitiser
entirely, so the loop is unreachable on the path essentially every member takes. The only door in is the
coin-your-own free-text field failing to sanitise twice. Real, worth fixing, not a Charter blocker. I had written
that caveat into this entry and still labelled it high — the label should match the evidence, not the alarm.

Third live persona walk. Chips were offered; the persona replied `"Untethered. Instinctive. Sovereign."` (a list,
correctly refused by the new punctuation guard), then `"Sovereign. Yeah. That one."` — and the beat re-prompted
her **fifteen consecutive times**, ending with *"Something is clearly stuck on your end… I'll wait."*

**The damning detail:** the state line shows `identityNoun="Sovereign"` set from her SECOND reply onward. We had
her answer and kept asking anyway. Two sources of truth disagreed — the model recorded the handle while the
engine's pick branch rejected the same message — and the engine's rejection won silently. That is the house
failure shape ([[member-words-outrank-model-guess]]) on a surface with no escape hatch: `pendingIdentityPick` is
never cleared, and unlike every other stage this path has no idle/runaway backstop, so it cannot self-recover.

**Real-UI exposure is LOWER than the walk suggests.** A tapped chip is pre-vetted and bypasses `sanitizeCoinedIdentity`
entirely, so it cannot loop. The only door in is the coin-your-own free-text field, twice failing sanitise. The
harness can't tap, so it took the one path a real member usually avoids. This is a genuine trap, not a common one.

**The punctuation guard from `fbbff7d` HAS NOW BEEN REVERTED** (superseding the earlier "do not revert" note here).
On review it was fixing a HARNESS artifact, not a member-reachable bug: the persona walk types, so it can emit
"Untamed. Alive. Sovereign."; a real member taps a pre-vetted chip and cannot. Meanwhile it widened the door to
this very loop. A cosmetic problem that doesn't occur, traded for easier access to a trap that does — net-negative,
so it's out. A guard there is reasonable to reconsider AFTER the escape below exists, not before.

**The fix, when someone rested does it:**
1. If `identityNoun` is already set, do not re-prompt — accept and advance. Kills the observed loop outright.
2. If the reply CONTAINS exactly one offered candidate, take it. `"Sovereign. Yeah. That one."` and `"I already
   picked — Sovereign."` both resolve, which is how real people actually answer a chip question in text.
3. Give the pick beat the runaway escape every other stage has, so no future input shape can dead-end it.

All three are deterministic and offline-provable — no prompt change, no live walk needed to prove them (though one
should follow anyway).

**Why it wasn't fixed on discovery:** found at the end of a very long session, after a change I'd made earlier that
day took prod down for two minutes. The failure mode at that point isn't finding bugs, it's confidently fixing the
wrong thing. Jay confirmed Jennifer had NOT hit this (he watched her walk), which removed the only argument for
touching the capture loop tonight.
