# Secrets — NEVER commit (AI Governance + CLAUDE.md: no keys, no member data)
.env
.env.*
!.env.example
*.pem
secrets/

# Node / Next.js
node_modules/
.next/
out/
dist/
build/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Supabase local
.supabase/
supabase/.temp/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local data / scratch — never let member data into the repo
/tmp/
*.local.*
member-data/

# Local dev database (pglite) and Next types
.pglite/
next-env.d.ts

# Vercel CLI link
.vercel

# Claude Code personal settings (bypass mode, etc.) — never commit
.claude/settings.local.json
