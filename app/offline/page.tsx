export const metadata = { title: 'Offline — Grinta for Life' };

export default function OfflinePage() {
  return (
    <div className="hero">
      <h1>You&apos;re offline</h1>
      <p>We can&apos;t reach the network right now. Nothing is lost — reconnect and pick up where you left off.</p>
    </div>
  );
}
